# VMS Development Plan

> **Status:** This is the repository's single controlling development plan, complete feature catalog, development sequence, approval ledger, implementation-status record, and verification checklist. README files describe how the current prototype works; retained files under `docs/` are approved or implemented technical contracts and do not maintain a second backlog or build order.
>
> **Reference coverage:** A structural and item-by-item audit against the supplied original 623-line vendor-research guide on 2026-07-19 confirmed that all 143 original P0-P12 roadmap items and their statuses are represented here. The copied research appendix was excluded after its capability families were mapped to the roadmap.

## Document Ownership and Third-Party Naming

- This file alone answers **what is approved, what is next, what is built, and what has been verified**.
- A technical specification may define how an approved item should behave, but it cannot change priority or status. If a specification conflicts with this file, this file controls.
- Existing prototype code is recorded separately from roadmap status. Code that predates approval does not make its corresponding feature `Approved`, `Built`, or `Verified`.
- The external reference guide is discovery material, not a second roadmap and not evidence that a third-party capability is supported.
- The public roadmap uses capability-based wording. Exact third-party product or service names belong only in dated evidence records, adapter manifests, compatibility matrices, procurement records, or integration specifications where identification is necessary.
- Standards and protocols such as ONVIF, RTSP, H.264, H.265, and SQLite may be named when they identify the exact technical contract under evaluation. Naming a standard or third party does not imply affiliation, endorsement, certification, or compatibility.
- Vendor prose, product catalogs, part numbers, commercial terms, regional claims, and hardware figures must not be copied into this roadmap. Any such claim must be independently verified under P0-14 and P12-11 before it affects product scope.

## Approval Contract

1. We will work through this checklist in dependency order, one approval item at a time unless an explicitly approved dependency requires a different order.
2. Before any product code is written for an item, Codex must discuss its problem, intended users, benefit, included and excluded scope, workflow, dependencies, data and security impact, measurable acceptance criteria, and important alternatives with the project owner.
3. The project owner must explicitly approve the agreed scope. Silence, an existing vendor feature, an existing implementation, or approval of another item does not count as approval.
4. Product code may start only when both the **Discussed** and **Approved** boxes for that item are checked and its decision is recorded in the Approval Log.
5. **Built** is checked only after implementation is complete. **Verified** is checked only after the agreed tests and acceptance criteria pass.
6. An item may instead be recorded as **Changed**, **Deferred**, **Rejected**, or **Out of scope**. Deferred or rejected work must not be implemented.
7. Benefits, capacity limits, commercial rules, regional availability, and safety/privacy claims are gates or acceptance criteria. They are not silently converted into product features.
8. Any capability depending on a vendor-specific product or managed service requires confirmation of the exact product, mode, version, device/firmware, API or SDK, license, tenant, target country, and support terms before approval.
9. Existing project behavior will be reviewed against the relevant item; it will not be assumed correct merely because it already exists.

### Status Legend

- **Discussed:** requirements and tradeoffs were reviewed with the project owner.
- **Approved:** the project owner explicitly authorized the recorded scope.
- **Built:** the approved scope was implemented.
- **Verified:** acceptance tests and evidence passed.
- **Type labels:** Feature, Integration, Architecture, NFR, Policy, Commercial, or Benefit.

Each roadmap line uses this order:

`[ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified`

For a Policy, Commercial, or Benefit item, **Built** means the agreed policy, configuration, evidence, or measurable acceptance target was put in place; it does not necessarily mean application code was added.

## Approval Log

| Item | Discussion date | Decision | Agreed scope | Acceptance criteria / evidence | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- |
| Structured audit + trusted-anchor boundary (P1-06 second slice) | 2026-08-09 | Approved by the operator's directive to build every remaining listed native capability according to worldwide standards; scope note `native/audit-structure-anchor-P1-06-proposal.md`; **built & verified on the dev box (2026-08-09)** as native increment 44 | Schema v15; backward-compatible canonical hash v1/v2; structured category/actor/subject/correlation and redacted before/after evidence; RFC 3161-ready SHA-256 chain-head request; persist a timestamp receipt only after injected cryptographic verification and unchanged-head confirmation | `vms_dbtest` 54/54; `--audit-selftest` 28/28 including migration/reopen, v2 hash chain, rejected unverified token, stale-head rejection, durable verified receipt, and exact-row tamper detection; all 13 workspace gates and root regressions pass; aggregate native CTest 10/11 only on the unchanged intermittent ONVIF loader failure and focused ONVIF passes | No bundled TSA or false trusted-clock claim. Production TSA/trust-root/revocation policy, producer integration for login/credential/live/export/vendor/entitlement events, and anchor-safe retention/checkpoint pruning remain. |
| Deterministic native voice invocation (P1-12 final slice) | 2026-08-09 | Approved by the operator's directive to build every remaining listed native capability according to worldwide standards; scope note `native/voice-command-P1-12-proposal.md`; **built & verified on the dev box (2026-08-09)** as native increment 43 | Recognizer-neutral W3C-SRGS-aligned boundary; explicit registered BCP-47 English grammar tags; confidence/ambiguity/no-match refusal; deterministic phrases to typed envelope arguments; voice cannot supply destructive confirmation; accepted and refused attempts retain `voice` source attribution without retaining arbitrary transcript text | `command_selfcheck` 50 checks and `--command-selftest` 40 checks; accepted speech drives the real workspace; unsupported language is side-effect-free; spoken confirm returns `needs-confirm`; all 13 workspace gates and root regressions pass; aggregate native CTest 10/11 only on the unchanged intermittent ONVIF loader failure and focused ONVIF passes | No microphone, speech provider, wake word, speaker identity, biometric authorization, fuzzy NLU/LLM, or multilingual claim. P1-12 is complete; recognizer/provider integration remains a deployment/privacy choice. |
| External control API rate limiting (P1-13 hardening slice) | 2026-08-09 | Approved by the operator's “what is there to build next, give me a list of all remaining items and features and capabilities, and continue building” directive; scope note `native/api-rate-limit-P1-13-proposal.md`; **built & verified on the dev box (2026-08-09)** as native increment 42 | Server-wide fixed-window budget across catalog, invoke, unauthorized, malformed, and unknown-route traffic; default/configurable 120 requests per 60 seconds; structured 429 response; transport rejection before command execution | `--api-selftest` 21/21 including 2 admitted + third rejected with unchanged workspace; non-positive CLI budget refused; all 13 workspace gates + root regressions pass; Release p95 0.0006/0.1136 ms; focused ONVIF passes despite the unchanged aggregate loader flake | Loopback/single-session-token scope only. Scoped identities, TLS, remote exposure, distributed/per-user limiting, streaming, and the AI-agent connector remain gated. |
| Continuity-preserving same-layout live re-plan (P3-14 remaining media optimization) | 2026-08-09 | Approved by the operator's “what is there to build next, continue building” directive; scope note `native/seamless-live-replan-P3-14-proposal.md`; **built & verified on the dev box (2026-08-09)** as native increment 41 | Brief PAUSED state boundary retaining the last Qt frame; replace only changed decode/queue/upload/compositor-pad branches; preserve unchanged decoders/pads; counted full-NULL fallback; layout changes remain full rebuilds | Release 64-tile H.265/D3D12 stress: 11 applies, 23 branch replacements, 0 full fallbacks, subsequent apply 25.2–70.8 ms, 16 playing at 29.2 FPS and 46/46 paused honest; all 13 workspace gates + root regressions pass; Release p95 0.0006/0.1265 ms; focused ONVIF passes despite the unchanged aggregate loader flake | Same-layout synthetic-source continuity only. No zero-latency crossfade, RTP-session migration, layout hot-swap, valid-credential camera proof, or low-end i5 calibration. |
| Site-scoped active alarm analysis (P3-18 sixth slice) | 2026-08-09 | Approved by the operator's “what is there to build next, continue building” directive; scope note `native/alarm-analysis-P3-18-proposal.md`; **built & verified on the dev box (2026-08-09)** as native increment 40 | Read-only join of current active device-health alarms to current device→site ownership; active/attention/severity/acknowledged/suppressed/occurrence/affected-device fields; immediate lifecycle refresh; site-switch isolation; explicit session-only coverage | `--site-operations-selftest` 32/32; Release offscreen QML smoke 160 ms with valid zero-alarm state and panel loaded; Release performance p95 0.0009/0.1717 ms; all 13 workspace gates + root regressions pass; focused ONVIF passes despite the unchanged aggregate loader flake | Completes P3-18's approved standalone scope. No VCA detections, durable event history, historical ownership, trends, cross-site rollup, correlation, or automatic actions. |
| Persistent decoded streaming duration (P3-18 fifth slice) | 2026-08-05 | Approved by the operator's “what is there to build next, start building” directive; scope note `native/streaming-duration-P3-18-proposal.md`; **built & verified on the dev box (2026-08-05)** as native increment 39 | Schema v14 premises totals; atomic overflow-safe Qt-free repository; monotonic observed `playing` branch-time; site-switch and restart isolation; separate recording/streaming evidence plus combined camera-time in the front-layer panel | `vms_dbtest` 54/54; `--site-operations-selftest` 30/30. Two Release persistent video runs advanced 18,708→29,248 ms across reopen with no downtime fill, QML 93 ms, 4 playing branches, 25.6 FPS; Release performance p95 0.0006/0.1377 ms; all 13 workspace gates + root regressions pass; focused ONVIF passes despite the unchanged aggregate loader flake | Active-premises decoded branch-time only. No per-camera history, transport/RTP time, stalled/paused time, remote/client reconciliation, ownership history, or analytics. |
| Cumulative completed-recording duration (P3-18 fourth slice) | 2026-08-03 | Approved by the operator's “what is there to build next, start building” directive; scope note `native/recording-duration-P3-18-proposal.md`; **built & verified on the dev box (2026-08-03)** as native increment 38 | Overlap-safe `SegmentIndex` camera-time; canonical timestamp validation; current active-site channel attribution; recording evidence in the front-layer panel; persistent streaming time remains explicitly unavailable | `vms_rectest` duration/validation gates green; `--site-operations-selftest` 29/29 including overlap removal, unassigned/site-switch exclusion, and retained disabled-channel history. Release record→index→panel smoke: 5 recorded seconds / 2 completed segments, 91 ms QML, 4 playing streams, 26.1 FPS; performance p95 0.0006/0.1120 ms; all 13 workspace gates + root regressions pass; focused ONVIF passes despite the unchanged aggregate loader flake | Current ownership attribution only; no historical ownership snapshot, persistent stream lifecycle, remote/NVR/cloud reconciliation, storage forecasting, or analytics. |
| Device-site ownership + reachability time (P3-18 third slice) | 2026-08-03 | Approved by the operator's “what is there to build next, start building” directive; scope note `native/device-site-uptime-P3-18-proposal.md`; **built & verified on the dev box (2026-08-03)** as native increment 37 | Schema v13 optional device→site ownership and monotonic reachability evidence; audited `device.site`; site-scoped device aggregates; honest last-seen and current reachability uptime | `vms_dbtest` 49/49; `--devices-selftest` 85/85; `--site-operations-selftest` 26/26 including site/unassigned filtering and restart honesty. Release persistent video smoke: schema v13, 92 ms QML, 6 assigned devices, 4 current reachable runs, persisted latest seen, 4 playing streams, 26.4 FPS; performance p95 0.0006/0.1322 ms; all 13 workspace gates + root regressions pass; focused ONVIF passes despite the unchanged aggregate loader flake | No inferred/automatic assignment, SLA/downtime history, clock sync, bulk editor, stream/recording duration, or analytics. |
| Operating hours and open-state (P3-18 second slice) | 2026-08-03 | Approved by the operator's “what is there to build next, start building” directive; scope note `native/operating-hours-P3-18-proposal.md`; **built & verified on the dev box (2026-08-03)** as native increment 36 | Schema v12 site weekly windows + local-date exceptions; audited premises schedule commands; UTC→IANA timezone evaluation with explicit DST semantics; read-only Open/Closed state and today's rule in the existing panel | `vms_dbtest` schema/reopen/overlap gates green; `--site-operations-selftest` 23/23 including spring-forward and repeated fall-back hour; authenticated API mutation reached the running panel; Release persistent video smoke loaded QML in 97 ms with `hours=Open`, panel loaded, 4 playing streams, and 24.0 FPS; performance p95 0.0006/0.1680 ms; all 13 workspace gates + root regressions pass; focused ONVIF passes despite the unchanged aggregate loader flake | No schedule editor UI, calendar import, cross-site inheritance, physical/media automation, uptime/last-seen, cumulative duration, or analytics. |
| Premises operations panel foundation (P3-18 first slice) | 2026-08-03 | Approved by the operator's “what is there to build next, start building” directive; scope note `native/premises-operations-P3-18-proposal.md`; **built & verified on the dev box (2026-08-03)** as native increment 35 | Read-only site aggregate: valid site-local clock, active premises/floor, device health counts, governed-media counts/FPS; explicit unavailable contracts for operating hours/open state, device uptime/last-seen, cumulative operating duration, and analytics | `--site-operations-selftest` 15/15; Release persistent video smoke loaded the panel in 103 ms with 6 source-accurate devices, 4 playing streams, and 24.6 FPS; performance p95 0.0010/0.2185 ms; all 13 workspace gates + root regressions pass; focused ONVIF passes despite the unchanged aggregate loader flake | No schedule schema/config, inferred uptime, fake analytics, history, alerts, multi-site rollup, new polling, mutations, or P3-18 later slices. |
| Honest live diagnostics (P3-04 first slice) | 2026-08-03 | Approved by the operator's “what is there to build next, start building” directive; scope note `native/live-diagnostics-P3-04-proposal.md`; **built & verified on the dev box (2026-08-03)** as native increment 34 | Observe real per-branch decoded state and compositor→Qt FPS; surface active codec/resolution; give every tile typed diagnostic availability; selected-camera panel shows FPS/codec/resolution and explicitly unavailable bitrate/latency/packet loss with reasons | Release 64-tile low-end smoke: 16 active/playing/FPS/media-known, 48/48 paused honest, 29.7 FPS, 92 ms QML. `--diagnostics-selftest` green; Release performance p95 0.0006/0.1691 ms; all 12 workspace gates + root regressions pass; focused ONVIF passes despite the unchanged aggregate loader flake | Current governed workspace source is synthetic and has no transport telemetry. No fake zeroes, RTSP credentials, RTCP probes, history, alerts, persistence, Playback diagnostics, or P3-18. |
| Adaptive spatial live tiles (P3-15 final media slice) | 2026-08-01 | Approved by the operator's “what is there to build next, start building” directive; scope note `native/spatial-live-tiles-P3-15-proposal.md`; **built & verified on the dev box (2026-08-03)** as native increment 33 | Progressive site pins → wing state cards → room live tiles. Reuse crops from the single governed grid composite through a shared-frame `VideoItem` source; no duplicate camera session/decode; live only for honest live/degraded states; state/FOV chrome retained | Release 64-tile low-end smoke: 64 delegates, 16 eligible = 16 consumers = 16 receiving, 223 frames, 16 unique crops, 48 tiles with no consumer, 85 ms QML load. Crop geometry self-tested; Release performance p95 0.0006/0.1874 ms; all 11 workspace gates + root regressions pass; focused ONVIF passes despite the unchanged aggregate loader flake | Does not add per-tile pipelines, real-camera credentials, snapshots, GIS, media-policy changes, P3-18, or compositor hot-swap. |
| Spatial performance acceptance (P3-15 third slice) | 2026-08-01 | Approved by the operator's “what is there to build next, start building” directive; scope note `native/spatial-performance-P3-15-proposal.md`; **built & verified on the dev box (2026-08-01)** as native increment 32 | A deterministic 64-tile headless benchmark of the shipped viewport→governor→model path, separate idle and forced-replan scenarios, CSV/human output, non-zero regression gate, p95 limits of 0.50 ms idle and 4.00 ms active, plus culled-FOV paint suppression and 64-tile offscreen smoke | Debug: idle p95 0.0026 ms / 0 replans; active p95 1.9852 ms / 2,000 replans. Release: idle p95 0.0006 ms / 0 replans; active p95 0.1995 ms / 5,000 replans. Release 64-tile QML load 105 ms; offscreen smoke clean. All 11 workspace gates and root regressions pass; 10/11 aggregate native components pass with unchanged intermittent ONVIF loader failure, and focused ONVIF passes | Excludes spatial live video/compositor work, map architecture changes, GPU/display profiling, >64 layouts, policy changes, and P3-18. |
| Premises + floor-plan/FOV (P3-15 second slice / A0) | 2026-08-01 | Approved by the operator's “what is there to build next, start building” directive; scope note `native/premises-spatial-P3-15-proposal.md`; **built & verified on the dev box (2026-08-01)** as native increment 31 | Schema v11 canonical site/floor + active Live selection; site name/timezone, floor name/optional plan URI/world dimensions; safe first-run default; persisted tile facing/FOV; audited `premises.site`, `premises.floor`, and `workspace.orientation`; spatial underlay/fallback, premises read-out, and FOV wedges | Native build; persistence schema/reopen/atomic workspace + premises/FOV round-trips; spatial and command selftests; coverage 0 violations; offscreen schema-v11 spatial smoke; root regressions green. Aggregate CTest: 10/11 green with the unchanged intermittent ONVIF Windows loader `0xc0000135`; focused ONVIF CTest passes | Explicitly excludes P3-18 analytics/operations, multi-site administration, upload/calibration, GIS, PTZ coupling, and live video under spatial tiles. P3-15 remains partially open for measured performance acceptance and the later live-video/map-tile decision. |
| Playback transport commands (P1-13 third slice / A0) | 2026-08-01 | Approved (scope) — owner directive “analyze entire project, what is there to build next, start building”; scope note `native/playback-command-P1-13-proposal.md`; **built & verified on the dev box (2026-08-01)** as native increment 30 | Close the last explicitly deferred discrete UI-mutation gap: add `playback.play/pause/speed/step/seek` and `replay.play/pause/speed/seek`, all under `playback.review`; route the Playback tab and instant-replay overlay buttons through the same structured command envelope/API; keep only in-progress scrub frames exempt and commit the final seek on release | `--command-selftest` drives the real Playback controller; `--coverage-check` reports 0 violations (39 mutators / 7 continuous or read-only sites) and no deferred-slice exemption; `--coverage-selftest`, Playback, instant, API, all 10 workspace gates, root regression suite, native build, and offscreen QML/video smoke pass | No schema/media change. Deferred: transport hardening, roles/auth, voice, new footage sources, synchronized playback. All 11 native component tests pass; aggregate CTest intermittently reports Windows loader `0xc0000135` for unchanged ONVIF, which passes in a focused CTest run. |
| P0-01 | 2026-07-15 | Approved, built, and verified | Open-source hybrid VMS; permission-ready Administrator-first design; recorder-owned footage; standalone plus optional self-hosted coordination/scale-out; large distributed deployments; priority-based device activity | Accepted Architecture Contract, documentation consistency audit, Administrator-only prototype migration, and full regression suite | P0-01 is complete; later gates remain unapproved |
| P0-02 | 2026-07-15 | Approved, built, and verified | Independent vendor-neutral VMS core with one identity-only reference adapter | Versioned async adapter contract, exact registry, identity-only reference shell, explicit port validation, arbitrary-adapter/no-fallback tests, documentation audit, and full regression suite | Real vendor/protocol operations remain unapproved |
| P0-03 | 2026-07-17 | Approved, built, and verified | Standards-compatible modular failure behavior for an optional VMS data/interaction layer; autonomous source devices and recorder-owned footage; honest degraded/read-only/unknown states; non-replayable physical commands; replaceable resilience policies | Normative failure-behavior contract, standards-correction audit, shared versioned policy modules, injectable backend facade, current-prototype honesty fixes, focused policy/integration tests, browser verification, and full regression suite | Concrete collectors, queues, databases, authentication, media bridges, access controllers, clusters, and VMS-owned recording remain later gates |
| C0 | 2026-07-19 | Build order approved; documentation consolidation built and verified; code consolidation pending | Build each shared domain rule, service primitive, and operational UI component once before adding another screen | C0 checklist below, duplicate-authority audit, reference-coverage audit, document-role cleanup, and full regression suite | C0 changes architecture, not unapproved product scope or the P0-04 persistence decision |
| N0 | 2026-07-20 | Approved (runtime/shell architecture direction) | Fully-native C++17 client — GStreamer (RTSP/RTP/ONVIF/WebRTC/HLS + pluggable HW decoders), Direct3D 11 zero-copy render, Qt/QML UI, SQLite local store, OS secret store (DPAPI) — as the delivered runtime; browser/Node retained as reference specification and backend test harness; centralized connection broker as the single device-connection identity; fully self-contained packaging (bundle Qt + GStreamer, user installs nothing) | `native/ARCHITECTURE.md` decision record; native increments 1–3 and 4a build and run verified on real hardware; self-contained package proven running with GStreamer and Qt removed from `PATH` (2026-07-20) | Records the runtime/shell decision only. Feature scope, capacity, media behavior, persistence (P0-04), and the decode governor (P3-03) remain their own P-phase gates. Native increments 1–4a are measurement spikes and check no feature boxes. Codec-licensing (P0-14/P0-17) is flagged, not cleared. |
| A0 | 2026-07-24 | Approved as an owner-directed product mandate | **Command-first + API-first architecture.** Every action the software can perform must be expressible as one validated command, invokable by UI, voice, and text, and exposed through a stable programmatic API suitable for external AI-agent control — with capability checks, confirmation of dangerous actions, result mapping, and audit. Extends P1-05 and P1-12 and adds P1-13 (external control API + coverage verification). | Owner directive 2026-07-24; per-scope implementation proposals precede code for each surface, and P1-13 acceptance criteria (complete action-to-command coverage; multimodal invocation; authenticated, capability-scoped, audited external API) | Mandate/principle approved; it shapes all native development from now on (every native action routes through the command envelope and is API-exposed). Concrete API/auth/transport, AI-agent connector, and coverage tooling are built under P1-05/P1-12/P1-13 with their own scope confirmation. |
| P3-03 | 2026-07-24 | Approved (scope) | First decode-governor pass per `native/governor-P3-03-proposal.md`: two-budget capacity model (decode-throughput + memory) seeded from `vms_hwprobe`, per-tier cost (main/sub/thumb/paused), admission control, degrade/recover with hysteresis, honest per-tile state; device priority used only as a pressure tie-breaker | Acceptance criteria in the proposal §10 (headline: requesting 64 main tiles must not crash — it caps and degrades); calibrated against `native/MEASUREMENTS.md` | Full P0-01L priority transitions, bandwidth budget, multi-monitor, and persisted config deferred to their own gates. Building as native increment 4b. |
| P0-04 | 2026-07-26 | Approved (scope) + **first pass built & verified** — owner selected the embedded, standalone-first direction | First persistence pass per `native/persistence-P0-04-proposal.md` (`vms_persist` + `vms_dbtest`/CTest, and `vms_workspace` persist/restore, all verified on the dev box): an embedded local relational store (SQLite, the selected embedded technology) as the **standalone** local authority behind the C0-03 adapter contract; versioned forward migrations; the entities the native client needs now (devices, cameras, `credential_ref` only, camera groups, operator settings, stream profiles, workspace layout state); backup/restore/export; honest failure per P0-03C | Acceptance criteria in the proposal §8 (durability across restart, idempotent forward migrations, atomic writes, backup/restore round-trip, export/import, honest failure, adapter-contract boundary) | Central DB, cloud, synchronization, and offline conflict reconciliation deferred to the coordinated gate (P1-10); credential **secrets** (DPAPI) are the next gate (connection broker + credential store); the remaining reference tables are deferred until a feature needs them. Persistence operations are built behind a clean interface so they can later be expressed as A0 commands (the command envelope itself is P1-13). Building as native increment 5. |
| Connection broker + credential store (DPAPI) | 2026-07-27 | Approved (scope) — owner selected **Option A** (in-process broker + Windows DPAPI); **increments 6a + 6b built & verified** | First broker/secret pass for the **standalone** runtime per `native/credential-broker-P1-03-proposal.md`: a Qt-free, SQLite-free, swappable `SecretStore` interface with a **Windows DPAPI** implementation (`CryptProtectData`/`CryptUnprotectData`; ciphertext at rest in an atomically-rewritten backing file, plaintext in memory only) and an in-memory test double; `device_credentials.credential_ref` resolved through it with atomic ref↔secret writes (6b); an in-process `ConnectionBroker` as the single device-connection identity — connect-by-`camera_id`, in-memory URL materialization, connection pooling + P0-03 backoff/circuit-breaker hooks, honest typed failure per P0-03C/E — with callers rewired off URL-with-password (6c). Realizes the N0 connection-broker decision and the secrets-storage clause of P1-03 for the standalone runtime | Acceptance criteria in the proposal §8 (no plaintext at rest, encrypt/decrypt round-trip + documented user/machine binding, single connection identity, atomic ref↔secret writes, honest typed failure, backoff/pooling, adapter-contract boundary) | **6a built & verified on the dev box (2026-07-27):** the `SecretStore` interface + DPAPI backend + in-memory double + `vms_credtest`/CTest (`credential_selfcheck`, 26 checks: interface contract, real DPAPI round-trip, no-plaintext-at-rest, durability across reopen, idempotent remove, typed `NotFound`/`Crypto`/`Misuse`); the P0-04 `persist_selfcheck` still passes after extracting the shared `Error.h`. **6b built & verified on the dev box (2026-07-29):** a `CredentialRepo` binds `device_credentials.credential_ref` ↔ `SecretStore` (SQLite holds only the opaque ref; the plaintext secret never enters any DB file), provisioning writes the ref row + encrypted secret atomically (both or neither — verified on both failure paths: a failed secret write rolls back the ref row, and an unknown-device FK violation writes no secret), the ref is stable across secret rotation, and remove is atomic + idempotent; `credential_selfcheck` now runs 45 checks (6a + 19 for 6b, incl. a no-plaintext scan of the main + WAL + shm DB files). **6c-1 built & verified on the dev box (2026-07-29):** the standalone `ConnectionBroker` (pure, Qt-free/GStreamer-free, lib `vms_broker`) is the single device-connection identity — `connect(camera_id, stream)` resolves device + credential-free URL template + secret, materializes the credentialed URL in memory (userinfo percent-encoded, never logged/persisted), enforces per-device pooling and the P0-03 backoff/circuit-breaker, and fails with honest typed errors (`NotFound`/`Crypto`/`Unavailable`/`Misuse`); `vms_brokertest`/`broker_selfcheck` passes 32 checks (all four CTest suites green). **6c-2 built & verified on the dev box (2026-07-29):** `vms_grid` connects by `camera_id` through the broker (`--broker-db`/`--provision`/`--camera-id`) — it provisions a camera (device + credential-free URL templates + secret via `CredentialRepo`) and resolves each tier's Main/Sub stream through the `ConnectionBroker` (URL materialized in memory only, never on the run CLI); `--broker-selftest` proves the wiring (5 checks). `vms_workspace` has no URL-with-password path to rewire yet (its `GridPipeline` uses pre-encoded synthetic clips, not RTSP) — it connects through the broker when it gains real camera video (a later media gate). This completes the broker gate's standalone scope (6a+6b+6c). **The broker path was confirmed against a live camera on 2026-07-29:** `--govern --broker-db --provision --camera-id` resolved the camera by id, materialized the credentialed URL in memory, connected to the real device, and surfaced honest per-tile typed failure (`Unauthorized` under placeholder credentials — proving the resolve→materialize→connect→auth chain end-to-end on a live device); hardware decode confirmation with valid credentials is the only remaining live check. Deferred: the coordinated broker/vault/RBAC (P1-10), operator authentication + password policy + MFA (rest of P1-03), durable credential audit (P1-06), the Linux secret store, and ONVIF/vendor credential negotiation (P2-05/P2-14). Built behind a clean interface so it can later be a validated command (A0/P1-12/P1-13). Building as native increment 6. |
| Recording backend + Playback footage (P4-03/P5-02) | 2026-07-29 | Approved (scope) — owner selected **Option A** (optional local recording as the first footage source); **standalone pass built & verified on the dev box (2026-07-29)** — 7a segment index (`recording_selfcheck` 29 checks) + 7b availability + 7a-media `vms_record` (5 real `.mp4` segments indexed) + 7c-1 `PlaybackController` (`--playback-selftest` 12 checks) + 7c-2 QML timeline/transport (offscreen smoke, no QML errors) + 7c-3 `PlaybackPipeline` (offscreen decode pulled 85 real frames); all 5 CTest suites green. Only the real-camera record path (original codec via depay through the broker) remains, sharing the live-camera block | First recording/playback pass for the **standalone** runtime per `native/recording-playback-P4-03-P5-02-proposal.md`: an **optional, off-by-default, deployer-located** local recording of broker-opened streams to segmented files with a SQLite `segments` index (atomic per-segment writes, typed errors, a crude size/age cap standing in for P4-06); a `FootageSource`/availability query returning honest available/missing/unknown/overlapping spans (P5-02, never presenting requested time as recorded); and the `vms_workspace` Playback instance wired to those segments with real transport (seek/speed/frame-step, single camera). Honors P0-01E (no mandatory central archive; recorders/devices remain authoritative; recording is optional and deployer-controlled). Building as native increment 7. **7a (segment index) + 7b (availability spans) built & verified on the dev box (2026-07-29):** schema v4 `segments`, `SegmentIndex` (atomic add with validation, overlap listing, oldest-first size/age retention returning trimmed files for cleanup), and `ComputeAvailability` (honest available/missing/overlapping spans that never present requested time as recorded); `recording_selfcheck` 29 checks, all 5 CTest suites green. 7a-media (GStreamer `splitmuxsink` recording feeding the index) and 7c (Playback wired to segments + real transport) remain. | Acceptance criteria in the proposal §8 (record→index, honest availability, requested≠recorded, real transport, optional+deployer-controlled, cap, store-contract boundary) | Selects optional local recording (Option A); recorder/NVR/ONVIF-replay retrieval (P4-03/P2-05/P0-14), schedules (P4-02), dual-stream profiles (P4-01), CVR/SAN (P4-04/05), retention loops/legal hold (P4-06), cloud (P4-07/08), storage-health (P4-09), synchronized multi-camera playback (P5-01), search (P5-03..07), and evidence export/chain-of-custody (P5-08/09) remain their own gates. Built behind clean interfaces so recording control + footage queries can later be validated commands/API reads (A0/P1-12/P1-13). |

| System optimization service (P12-03/P0-01F) | 2026-07-29 | Approved (scope) — owner selected **Option A** (in-process health sampler feeding the governor; OS-service + node-failover deferred to their own gates), building as native increment 8; **built & verified on the dev box (2026-07-29)** — `SystemHealthProbe` + `Optimizer` (headroom→profile, anti-flap deadband) + bandwidth admission budget + honest report (`vms_opttest` 16 checks, all 6 CTest suites green), and the live `vms_workspace` sampler degrades the running wall under injected RAM pressure (`--opt-fake-ram 400` → `decoding 9/16` at a clamped 240 MB budget) and recovers with ample headroom. Remaining: true active bandwidth probing (operator/estimated for now) | First optimization pass, standalone runtime per `native/optimization-service-P12-03-proposal.md`: a Qt-free swappable `SystemHealthProbe` (Windows impl + injectable fake) sampling live RAM/CPU headroom; an `Optimizer`/`OptimizerSession` that maps live headroom to the governor's `CapacityProfile` (never above the static ceiling) with a deadband so the budget does not flap; a **bandwidth budget** added as a third governor admission budget; and an honest, read-only optimization report naming the binding budget. No automatic OS mutation (recommend-only). `vms_opttest`/CTest. | Acceptance criteria in the proposal §7 (proactive degrade before exhaustion, recover without flapping, bandwidth admission, honest report, bounded sampler cost, no OS mutation, boundary) | Selects the in-process sampler only; the always-on OS service and node-connectivity failover remain their own (security-reviewed / P1-10) gates. Sequenced after recording+playback (owner 2026-07-29). |

| Node-connectivity resilience (P1-10/P0-03/A0) | 2026-07-29 | Approved then **Withdrawn/removed at owner request (2026-07-29)** | A standalone node-failover decision core (`NodeRegistry`/`EndpointHealth`/`FailoverPolicy`) was approved and built (native increment 9, `vms_nodetest` 21 checks) but the owner then removed it: the standalone runtime has no peers to fail over between, so node-connectivity resilience belongs to the coordinated deployment. The code was reverted before commit. | — | Superseded by the **P1-10 node-transport + AI-agent connector proposal** (`native/node-transport-P1-10-proposal.md`), which will design node connectivity — including endpoint failover and active bandwidth probing — where real peers exist. Camera/NVR/DVR connectivity is unaffected (that is the ConnectionBroker). |

| Device onboarding (P2-01/P2-02/P2-05) | 2026-07-29 | Approved (scope) — owner directed "build according to the plan, as you recommend, standards-compliant" (2026-07-29); building as native increment 10; **built & verified on the dev box (2026-07-29)** — `DeviceRepo` (`vms_devtest`/`device_selfcheck`, 26 checks incl. an onboard→`ConnectionBroker` end-to-end credential resolve; all 7 CTest suites green). **Onboarding UI built & verified as native increment 14 (2026-07-29):** a `DeviceController` + Devices tab in `vms_workspace` surfacing onboard / honest per-device health / acknowledge / maintenance / remove (`--devices-selftest` 17 checks, offscreen smoke clean, all 9 CTest suites green) | First device-onboarding pass for the **standalone** runtime: a `DeviceRepo` on the P0-04 store that **onboards** a recorder / direct IP camera (id, name, address, vendor) with its camera channels (stable ids, credential-free main/sub URL templates) and its credential — the secret stored through the `SecretStore` under the **same `credential_ref` scheme the ConnectionBroker resolves** — **atomically** (P2-01); **auto-creates/reconciles exactly one protected default device group per device** tied to the stable device id (P2-02); and **idempotently syncs channels** (add / update / rename / disable / remove) **preserving stable camera ids and operator-customized names** (P2-05). This is the standards-correct inventory layer, **not a protocol mock**; real ONVIF discovery/negotiation (P2-03/P2-14) will feed this repo as a data source in a later slice with live-camera verification. `vms_devtest`/CTest including an onboard→broker end-to-end credential resolve. | Acceptance: atomic onboard (device+cameras+group+credential all-or-nothing, honest typed failure), the onboarded credential resolves through the `ConnectionBroker`, idempotent re-onboard, channel sync preserves operator names + stable ids and reconciles the default group on add/remove | ONVIF/LAN discovery + interoperability (P2-03/P2-04/P2-14), camera capability families (P2-07), device health/lifecycle (P2-13), remote support actions (P2-12), and infrastructure/physical/mobile device families (P2-08…P2-11) remain their own gates. |

| ONVIF discovery + interoperability (P2-03/P2-14) | 2026-07-29 | Approved (scope) — owner sequenced ONVIF → device health → onboarding UI (2026-07-29); building as native increment 11; **built & verified on the dev box (2026-07-29)** — increment 11 (WS-Discovery) + increment 12 (device/media SOAP client): `vms_onviftest`/`onvif_selfcheck` 25 checks (standards-exact Probe + prefix-agnostic ProbeMatch parse + dedup + scope decode; WSSE UsernameToken **PasswordDigest** = Base64(SHA1(nonce+created+password)) verified independently, GetProfiles/GetStreamUri/GetDeviceInformation request envelopes + response parsers against real device fixtures), all 8 CTest suites green. **Discovery→onboard UI built & verified as native increment 16 (2026-07-30):** an injectable `DiscoverySource` fronts the two ONVIF transports so the Devices tab drives them — `DeviceController` gains a `discoveredDevices` model + `startDiscovery`/`onboardDiscovered`, a pure `chooseStreams` profile→Main/Sub mapping, `deviceIdFromHost`, and duplicate detection against the inventory (P2-03); one-click onboard fetches profiles+RTSP and onboards atomically via `DeviceRepo` (`--devices-selftest` 36 checks, offscreen smoke clean, all 9 CTest suites green). **Remaining (live/hardware):** the UDP `Discover()` on the LAN and the HTTP `FetchDevice()` against the test camera → `DeviceRepo.onboard` (the live `DiscoverySource` drops in unchanged); multi-channel ONVIF encoders + bulk/activation-state selection are later slices | Standards-exact ONVIF onboarding **source** feeding `DeviceRepo` (increment 10). First slice = **WS-Discovery** (OASIS WS-Discovery over SOAP-UDP multicast to 239.255.255.250:3702, ONVIF `dn:NetworkVideoTransmitter` probe): a pure `BuildProbe` + `ParseProbeMatches` (namespace-prefix-agnostic; extracts EndpointReference / Types / Scopes / XAddrs) + dedup-by-endpoint + scope decoding, verified against **real ONVIF ProbeMatch fixtures**; plus a `QUdpSocket` transport (`Discover(timeout)`) for live LAN discovery. **Not a mock** — the real ONVIF wire format. `vms_onviftest`/CTest for the pure core; live LAN discovery + the later device/media SOAP client (`GetDeviceInformation`/`GetProfiles`/`GetStreamUri` with WS-UsernameToken, populating `DeviceRepo`) are verified on hardware. | Acceptance: a standards-conformant Probe (parses clean; correct Action/To/Types), ProbeMatch parsing that survives prefix variation and extracts EPR/Types/Scopes/XAddrs, dedup by endpoint, honest empty on malformed input; (hardware) live discovery finds the test camera | The ONVIF media/PTZ SOAP services beyond onboarding, vendor adapters, and the conformance matrix remain P2-14/P2-07 gates. |

| Rename/detach/delete (P2-06, first slice) | 2026-07-30 | Approved (scope, by owner directive) — owner directed "what is there to build next, start building" (2026-07-30), the same standing "build the next thing as you recommend, standards-compliant" grant used for the device family; building the unambiguous slice as native increment 20; **built & verified on the dev box (2026-07-30)** | P2-06 slices for the **standalone** runtime. Inc 20: **device rename preserving all associations** (`DeviceRepo::renameDevice` — updates the name + default-group display name; cameras, stable ids, operator names, group membership, and credential untouched; honest `NotFound`) and a **confirmation gate on destructive Remove** (two-step Remove → Confirm/Cancel, replacing the one-click remove). Inc 21: **device attach/detach** (schema v8 `devices.disabled`; `setDeviceDisabled` empties the default group while preserving all config; `pollHealth` skips detached devices; Attach/Detach UI + dimmed card + badge). `DeviceController` + `DevicesView.qml`. `--devices-selftest` (68 checks) + offscreen smoke. | Acceptance: rename preserves associations + refreshes the group name + honest error on a bad id; destructive remove requires an explicit confirm; detach preserves config but excludes from active use (empty group, poll skipped) and re-attach restores it | Inc 22: **rescan a camera from discovery** (`rescanDevice` re-fetches a direct camera's ONVIF profiles from the matched discovered device and reconciles its channel via `syncChannels`, preserving the operator name + stable id; refuses recorders). `--devices-selftest` (73 checks). | Acceptance (additionally): rescan refreshes a camera's streams from its discovered device, preserving the operator name + stable id, and refuses a recorder / an unmatched device. | **Deferred for an explicit scope decision:** reduced-channel handling beyond `syncChannels`, and rename/detach for individual channels. Built behind clean repo/controller methods so they can later be A0 commands. |
| Device health & lifecycle (P2-13) | 2026-07-29 | Approved (scope) — owner sequenced #2 (2026-07-29); building as native increment 13; **built & verified on the dev box (2026-07-29)** — `HealthMonitor` (`vms_healthtest`/`health_selfcheck`, 22 checks; all 9 CTest suites green) | A pure, Qt-free `HealthMonitor` decision core: per-device health tracked as **separate honest dimensions** — reachability, stream, storage (per the P0-03 rule that keeps reachability/function/recording/storage state distinct) — deriving an overall state (Unknown / Online / Degraded / Offline / Unsupported, honest **Unknown until observed**); **exceptions** raised on a bad state with a first-seen time, operator **acknowledgement** (suppresses notification, exception persists until recovery), **auto-clear on recovery**, and **maintenance windows** that suppress notification; a firmware-inventory string; `needsAttention()` / `shouldNotify()` for the alerting surface; injectable clock for deterministic tests. Fed by the ConnectionBroker's connect outcomes now and ONVIF/live polling later. `vms_healthtest`/CTest. | Acceptance: honest Unknown until observed; distinct dimensions; correct state derivation; exception raise → acknowledge → auto-clear lifecycle; maintenance suppression; notification set correct | The real device-polling loop, storage/firmware collection from devices, the notification transport (P6-08 health events), and the health UI are their own slices. |
| Command coverage (P1-13 second slice / A0) + secret redaction (P1-09) | 2026-07-31 | Approved (scope) — owner directive 2026-07-31 (standing grant); scope note `native/command-coverage-P1-13-proposal.md`; building as native increment 29; **built & verified on the dev box (2026-07-31)** — `--coverage-check` 0 violations, `--coverage-selftest` 10 checks (incl. the negative test), `command_selfcheck` 37 checks; all 11 CTest suites + 10 workspace gates green | Closes P1-13's **enforcement** half: `CommandController::invoke(id, argsMap, confirm)` adds the **structured** invocation path QML clicks need (typed args — names with spaces, URLs, credentials — without the text leg's tokenization); 12 new commands cover the remaining discrete surface (`device.onboard/onboardRecorder/onboardDiscovered/discover/rescan/acknowledge/maintenance`, `channel.rename/disable/remove` (dangerous) `/move`, `workspace.place`); and **all 27 direct controller call sites in the shipped QML are rerouted through the envelope with no direct fallback**. The enforcement itself is a pure `CoverageCheck` that scans the **shipped** QML from the Qt resource against a declared 39-mutator table and **exits non-zero on any bypass** (`--coverage-check`, CI-gateable); exemptions are declared, categorized and printed (pure reads; continuous viewport/scrub frames, whose *committed* gesture is itself a command; and the Playback transport, named as a deferred slice rather than hidden). **A P1-09 security defect was found and fixed by this work:** an end-to-end run showed a device password reaching the durable audit in plaintext; `ParamSpec::secret` plus a name-based backstop now redact secrets to `***` in every audit row, log line, and catalog entry, while the handler still receives the real credential (verified by a raw byte-scan of the audit DB). | Acceptance (proposal §Acceptance): zero violations on the shipped UI; the checker **detects** an injected bypass at the exact line; every exemption explicit and printed; device actions produce durable audit rows and `channel.remove` refuses without confirm; all prior self-tests green — verified 2026-07-31 | Deferred at increment 29: the voice leg (P1-12), transport hardening (scoped tokens, TLS, rate limits, streaming — rest of P1-13), and roles behind the capability strings (P1-02/P1-03). Playback-transport commands were subsequently closed by increment 30. The check is a test/CI gate, not a compile-time lint. |
| Durable audit history (P1-06 first slice) | 2026-07-31 | Approved (scope) — owner directive 2026-07-31 (standing grant); scope note `native/audit-log-P1-06-proposal.md`; building as native increment 28; **built & verified on the dev box (2026-07-31)** — `--audit-selftest` 18 checks; end-to-end: the app driven over the API then exited, `--audit-dump` read back the executed command AND the refused destructive attempt with `[api]` attribution and an intact chain; all 11 CTest suites + 8 workspace selftests green | First P1-06 slice for the **standalone** runtime: schema **v10** `audit_log` + a Qt-free `AuditRepo` in `vms_persist` — **append-only by contract** (no update/delete API exists at this layer) with every row **hash-chained** to its predecessor (`row_hash = SHA-256(prev_hash ‖ length-prefixed fields)`, hash injected so the repo stays dependency-free), so editing or deleting any row breaks `verifyChain` from exactly that row (**tamper evidence**). The A0 envelope's audit sink also appends durably whenever the store is open: every attempt from every surface — **palette / api / ui** source-attributed, **refusals included** — lands one row. `--audit-dump [N]` is the operator/compliance read (rows + chain verdict). | Acceptance (proposal §Acceptance): both executed and refused attempts land durably and survive a reopen; the untouched chain verifies and a direct-UPDATE tamper is detected at the exact row; no update/delete surface; forward-only v9→v10 migration with all prior suites green; `--audit-dump` newest-first with source attribution — verified 2026-07-31 | Deferred (rest of P1-06): login/credential/export auditing (needs P1-03 operator auth), configuration before/after values, **trusted external timestamps** (local clock stamps this slice), retention/rotation policy, and chain-head signing/anchoring (tamper *evidence* is claimable locally; tamper *proof* is not, and is deferred with the coordinated gates). |
| Event rules & alarm lifecycle (P6-03/P6-07 first slice) | 2026-07-31 | Approved (scope) — owner directive 2026-07-31 (standing grant); scope note `native/events-alarms-P6-proposal.md`; building as native increment 27; **built & verified on the dev box (2026-07-31)** — `event_selfcheck` 30 checks + `--alarms-selftest` 17 checks end-to-end (real DeviceController signal path); all 11 CTest suites + 7 workspace selftests green; offscreen smoke clean | First events/alarms pass for the **standalone** runtime: a pure Qt-free `AlarmEngine` (lib `vms_events`) — normalized events → enabled rules (event-type trigger + optional device filter, priority mapping, optional auto-clear event) → **deduplicated, accountable alarms** (new → acknowledged (silenced, persists across re-ingest) → escalated → cleared; re-occurrence after clear = a fresh alarm), maintenance suppressing **notification never state**, honest needsAttention/shouldNotify, injectable time. **Notifies, never actuates** (P6-05 is its own gate). First event source: `DeviceController::healthTransition` (offline/degraded/recovered, transitions only) from the live health feed, under three default rules. `AlarmController` + 🔔 Alarms chip/panel; lifecycle verbs registered in the **command envelope** (`alarm.ack/escalate/clear`) so the panel, palette, and external API are one audited gate — the panel is the first UI surface natively routed through commands (the P1-13 coverage direction). | Acceptance (proposal §Acceptance): rule matching incl. filters + disabled rules; dedup with ack-persistence; lifecycle incl. auto-clear on recovery + fresh-after-clear; maintenance suppression honest; a live transition raises the alarm end-to-end and `alarm.ack` through the envelope acknowledges it, audited; offscreen smoke + all prior selftests green — verified 2026-07-31 | Deferred to their own gates: **P6-05 automated linkage** (separately approved actions), P6-04 durable event/alarm pipeline + measured throughput, rule schedules/correlation/simulation/versioning (rest of P6-03), delivery channels/retry/SLA (rest of P6-07), ARC (P6-09/10), and event sources beyond device health (motion/VCA/inputs). |
| External control API (P1-13 first slice / A0) | 2026-07-31 | Approved (scope) — owner directive 2026-07-31 (standing grant, continuing the A0 arc from increment 25); scope note `native/command-api-P1-13-proposal.md`; building as native increment 26; **built & verified on the dev box (2026-07-31)** — `--api-selftest` 16 checks; proven end-to-end: an external client discovered the 11-command catalog and drove the running app over HTTP (layout resize + focus), was blocked 409 on an unconfirmed destructive command and 401 without the token, all audited; all 10 CTest suites + 5 workspace selftests green | First P1-13 slice for the **standalone** runtime: `CommandServer` (Qt HttpServer) puts an HTTP/JSON transport in front of the increment-25 envelope — **loopback-only** (127.0.0.1, never public), **off by default** (`--api-port`), **bearer-token** authenticated on every route (random per-session token printed once; `--api-token` for scripted runs). `GET /v1/commands` = machine-readable catalog (agent discovery); `POST /v1/invoke` flows through the **same** registry gate as the palette (validation → capability → dangerous confirm → audit; the agent is a client of the envelope, never a privileged path) with deterministic status mapping (200/404/400/403/**409 needs-confirm**/500) and one `{outcome,message}` body shape. | Acceptance (proposal §Acceptance): 401 without a valid token; catalog discovery; a valid invoke moves the real workspace; deterministic refusal mapping; dangerous commands blocked without confirm over the wire; every attempt audited; loopback-only + opt-in — all verified 2026-07-31 | Deferred (rest of P1-13): multi-user/scoped tokens, TLS, rate limits, result/event streaming, non-loopback exposure (its own security-reviewed decision), the AI-agent connector (a client of this API), and the UI-coverage check that closes the gate. Durable audit = P1-06. |
| Unified command envelope (P1-12 first slice / A0) | 2026-07-31 | Approved (scope) — owner directive 2026-07-31 "what is there to build next, start building" (the standing "build the next thing as you recommend" grant); scope note `native/command-envelope-P1-12-proposal.md`, per the A0 rule that a per-scope proposal precedes code; building as native increment 25; **built & verified on the dev box (2026-07-31)** — `command_selfcheck` 32 checks + `--command-selftest` 18 checks; all 10 CTest suites green; spatial/instant/playback/devices regressions green; offscreen smoke clean | First buildable slice of the **A0 mandate / P1-12** for the standalone runtime: a pure Qt-free `CommandRegistry` (lib `vms_command`) where every action is a typed `CommandSpec` (stable id, params with ranges/enums, capability, dangerous flag) and every invocation flows through **one gate** — validation with typed refusals (nothing executes on a mismatch), **capability membership** (P0-01A: the Administrator session grants all; the check makes later RBAC a data change, not a rewrite), **dangerous-action confirmation**, execution, deterministic result mapping — and an **audit record for every attempt, refused or executed**. A deterministic **text leg** (`invokeText`, positional binding + trailing `confirm`) and a **machine-readable JSON catalog** (P1-13's discovery seed, `--commands`). `CommandController` registers the native verbs over the live controllers (workspace focus/quality/priority/layout/sweep/spatial, replay start/live, device rename/detach/**remove (dangerous)**) and a **⌘ Command palette** (Ctrl+K) in the workspace is the first non-click invocation surface, showing results + the session audit trail. | Acceptance (proposal §Acceptance): typed refusals never execute; capability denied without the grant; dangerous refuses without confirm and executes with it; text lines drive the real workspace; every attempt audited; catalog machine-discoverable; offscreen smoke + prior selftests green — all verified 2026-07-31 | Deferred to their own gates: the external control API/auth/transport + UI-coverage check (P1-13), the voice leg (P1-12), durable immutable audit (P1-06), real roles (P1-02/P1-03), and rewiring every existing QML click through the envelope (P1-13 acceptance). |
| Spatial camera canvas (P3-15 slice 1 + P3-01 drag) | 2026-07-31 | Approved (scope) — owner directive 2026-07-31: rebuild the prototype's spatial canvas UI "same or better" natively; scope note `native/spatial-canvas-P3-15-proposal.md`; building as native increment 24; **built & verified on the dev box (2026-07-31)** — `--spatial-selftest` 33 checks; `persist_selfcheck` extended (schema v9 + position round-trip); all 9 CTest suites green; playback/instant/devices regressions green; offscreen `--spatial` smoke clean incl. a real v8→v9 migration | First spatial-navigation pass for the **standalone** runtime, porting the reference prototype (`spatial-canvas.js`) **exactly**: a Grid \| Spatial toggle on the Live workspace opens a pannable, zoomable canvas (wheel zoom toward cursor 0.15–6×, drag-space pan, click focuses); a pure `SpatialPolicy.h` core carries the prototype's zones (focus ≤24% / peripheral ≤55% / prewarm 240 px / culled), zoom levels (site <0.4 / wing <0.9 / room) with tier caps, and the anti-flap dwell (downgrade immediately, promote 300 ms after settle). **Better than the prototype:** `WorkspaceController::updateSpatialViewport` drives the **real governor** in one batched re-plan (honest decode/mem/bw admission replaces the prototype's crude stream cap; a culled tile is `visible=false` → PausedOffscreen, genuinely not decoding); **per-tile drag repositions a camera** (P3-01's drag verb — the prototype only panned); dragged positions **persist** (schema v9 `workspace_tile.pos_x/pos_y`, forward-only additive, −1=unset→prototype grid default); honest P3-14 state chrome on every spatial tile, map-pin dots at site zoom. | Acceptance (proposal §Acceptance): prototype policy parity; one batched re-plan per viewport pass with no-change passes free; honest culling; drag + persisted positions surviving restart; clean offscreen smoke + prior selftests green — all verified 2026-07-31 | Deferred: live video under the spatial tiles (fixed-geometry d3d11 compositor; the prototype likewise drew snapshots, not live video), floor-plan/map underlays + camera facing/FOV cones + the premises/site entity (rest of P3-15), saved/personal/shared named layouts (rest of P3-01), RBAC (P1-02). Built behind `Q_INVOKABLE`s so it can later be A0 commands. |
| Instant playback from live (P3-05) | 2026-07-31 | Approved (scope) — owner selected this lane on 2026-07-31 and directed the build under the standing "build the next thing, as you recommend, standards-compliant" grant; scope note `native/instant-playback-P3-05-proposal.md`; building as native increment 23; **built & verified on the dev box (2026-07-31)** — `InstantReplayController` (`--instant-selftest`, 22 checks; `--playback-selftest` + `--devices-selftest` regressions green; offscreen QML smoke clean, instant decode pulled 89 real frames) | First **instant-replay** pass for the **standalone** runtime: from the Live wall, jump back N seconds on the recording-backed camera and watch, then return to live. `InstantReplayController` **owns** a `PlaybackController` (the honest availability/playhead/transport model, inc 7c-1) pinned to a look-back window and feeds a dedicated `PlaybackPipeline` (inc 7c-3) → its own `instantVideoOut` — **reusing** the recording machinery, no duplication. `replay(seconds)` computes the camera's live edge (latest recorded moment), opens `[edge − seconds, edge]`, positions the playhead at the window start, and auto-plays; `returnToLive()` closes the overlay + pauses. UI: a "10s/30s/60s" control on the Live info panel + a layered replay overlay over the still-running live wall (P3-14 layering) with a compact transport and a "⟵ Live" return. `Q_INVOKABLE` so it can later be an A0 command. Read-only over the **local** recording index (P0-01E); honest per P0-03. | Acceptance (`native/instant-playback-P3-05-proposal.md` §Acceptance): replay opens a window ending at the live edge with the playhead on real footage and auto-plays; a look-back longer than the footage clamps to the earliest recorded moment and reports the actual look-back; an unrecorded camera is honestly unavailable with no fabricated spans (requested time never shown as recorded); return-to-live deactivates + pauses; offscreen smoke loads the overlay with no QML errors and the replay decode pulls real frames | Deferred: mapping an **arbitrary** live tile → `camera_id` (the live media↔inventory binding, a later media gate — this pass replays the `--rec-camera`), recorder/NVR/ONVIF-replay and cloud instant retrieval (P4-03/P2-14/P4-07), synchronized multi-camera instant replay (P5-01), look-back permissions/RBAC (P1-02/03), and evidence capture (P5-08/09). Built behind a clean `Q_INVOKABLE` controller so it can later be a validated command (A0/P1-12/P1-13). |

## Cross-Cutting Product Mandates (owner-directed 2026-07-24)

These are standing requirements that constrain every feature; they are not a single build item.

### Quality Bar

The product must be **best-optimized, low-latency, lightweight, and robust**. Concretely, for
every delivered capability: no wasted decode/CPU/GPU/memory (only the working set does expensive
work); honest capacity limits with graceful degradation instead of freeze or crash; a minimal
dependency and memory footprint; and correct behavior under device, network, and resource failure
(the P0-03 contract). These convert into measurable acceptance targets under P0-18 and P12-03 and
apply to native increments as they are built.

### A0 - Command-first, API-first, AI-agent-controllable

Every action the software can perform — every button, gesture, menu, and automatic operation that
changes state — must be expressible as **one validated command**, invokable by **UI, voice, and
text**, and exposed through a **stable programmatic API** suitable for an external **AI agent** to
drive the software. Commands carry capability checks, confirmation for dangerous/physical actions,
deterministic result mapping, and audit boundaries (P1-06). This extends the existing command
envelope work (P1-05, P1-12) and is verified by P1-13. It is a design constraint from now on: native
UI actions route through the command envelope rather than calling internals directly, so the same
action is reachable by a click, a spoken phrase, a typed command, or the agent API.

## Immediate Track C0 - Build Once, Use Everywhere

> The owner approved this consolidation order on 2026-07-19. C0 removes duplicate implementations and contradictory contracts before feature delivery resumes. It may refactor current adapters and prototype UI, but it must not select production persistence, add a real media bridge, or implement another unapproved product feature.

- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **C0-01 [Architecture] One development authority.** Keep every feature, delivery sequence, approval status, carryover item, and verification check in this file. README files remain operational descriptions; technical documents remain reference specifications without their own build orders.
- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **C0-02 [Architecture] Canonical inventory contracts and reconciliation.** Create one versioned device/camera/group model, one legacy upgrader, one stable ID policy, one device-channel reconciliation function, and one system-device-group builder. UI, API, services, JSON storage, and future SQL mappings must consume those contracts rather than reinterpret them. **Built and verified 2026-08-09:** `shared/inventory/contract.js` is the version-2 browser/CommonJS authority for upgrade, normalization, stable IDs, reconciliation, and protected device groups; browser, API, service, storage snapshots, and tests import it. The complete `node test-all.js` compatibility suite is green.
- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **C0-03 [Architecture] Transactional current persistence boundary.** Move atomic replace, rollback, declared-table schema, and typed persistence errors behind the current store adapter. Routes must not reach into adapter internals. This refactor must remain technology-neutral and must not pre-approve SQLite or any central database under P0-04. **Built and verified 2026-08-09:** the current file adapter owns synchronous transactions, rollback, temp-file/fsync/rename replacement, declared tables/records, snapshots, replacement, and typed schema/unavailable errors. Inventory routes use only adapter methods. Dedicated adapter tests and the complete compatibility suite pass.
- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **C0-04 [Architecture] One operation and resilience vocabulary.** Centralize operation IDs; explicitly map adapter, policy, command, persistence, and HTTP outcomes; convert legacy health observations at their boundary; and make command/device execution consume the shared resilience policy service. **Built and verified 2026-08-09:** device integrations and commands use the shared operation registry and `ResiliencePolicyService`; adapter, policy, command, persistence, and HTTP outcomes use explicit tested mappings; legacy device-health booleans convert once into the dimensional health contract. Missing-expiry PTZ is blocked before adapter dispatch. Full compatibility suite passes.
- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **C0-05 [Architecture] Shared policy and utility primitives.** Use one browser/Node media policy, one stream-profile source, and shared configuration, validation, time, identifier, error, stable-sort, and policy-registry primitives where semantics are truly identical. **Built and verified 2026-08-09:** `shared/media-policy.js` owns browser/Node tiers, bitrates, capacity guards, debounce, and default stream profiles; `shared/core.js` owns identical timestamp, cryptographic ID, stable-sort/stringify, and coded primitive-error semantics. The versioned `shared/media-capabilities.js` registry now separates standards catalog membership from per-operation runtime support, requires dated probe/conformance evidence before `supported`, negotiates only a verified local/remote intersection, and permits explicit future/vendor extensions without fallback guessing. Inventory validation and resilience registries remain their appropriate domain authorities. Full compatibility suite passes.
- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **C0-06 [Architecture] Repository and composition boundary.** Define repository interfaces for the canonical domain, implement the current file-store adapter, inject repositories/services at one composition root, and remove parallel service construction. **Built and verified 2026-08-09:** real device/camera/group/profile/session/inventory repositories replace throw-only stubs; the file-store repository context is the current adapter; `backend/composition.js` constructs one repository, resilience-policy, and service graph used by server, CLI, and checks. Canonical services receive repositories and onboarding reuses the root device/camera services. Full compatibility suite passes.
- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **C0-07 [NFR] Shared test infrastructure.** Move to one test runner style with reusable memory database, HTTP server, app loader, and domain factories; keep `node test-all.js` as the complete compatibility entry point. **Built and verified 2026-08-09:** `test-support/` provides the common harness, loaded memory database, HTTP server/request lifecycle, browser-app VM loader, and canonical domain factories; inventory/workspace, onboarding, and API tests consume them. `node test-all.js` remains the single complete entry point and passes.
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **C0-08 [Architecture/Feature] One operational workspace component family.** Replace separate Live and Playback trees, rows, grids, toolbar handlers, popovers, fullscreen logic, workspace defaults, and legacy CSS overrides with two independently stateful instances of one configurable component family. Playback-only controls remain mode extensions. **In progress 2026-08-09:** `shared/operational-workspace.js` now generates both runtime DOM trees from one configuration/component/controller, with Playback as a mode extension; the duplicate static trees are removed; resource-tree, camera-row, fixed-grid, and drawer rendering are shared; overlay, layout, custom-division, close-all, fullscreen, tabs, defaults, limits, and persistence controls use common mode-configured paths. Mode-only zoom/PTZ/settings and Playback range/transport/evidence remain extensions. Source regressions and live browser checks pass at default and 800×650 viewports. Remaining before Built/Verified: consolidate the spatial-canvas controller path, mode-specific popover listener lifecycle, legacy CSS overrides, and the complete fullscreen/float/stacking/listener/availability browser matrix.

### C0 Verification Checklist

- [x] The external reference and this guide contain the same 143 original P0-P12 items and the same pre-consolidation status.
- [x] The copied research prose, product catalog, part numbers, vendor-derived hardware/capacity tables, commercial claims, and regional claims are not embedded in the controlling roadmap.
- [x] Every repository document declares a single role: controlling roadmap, current-use README, normative approved contract, or non-authorizing reference specification.
- [x] No repository document outside this file maintains an independent build order, backlog, Definition of Done, or feature-status checklist.
- [x] One inventory contract version and upgrader are consumed across browser, API, service, storage, and tests.
- [x] Device channel reduction, detachment, IDs, aliases, and automatic groups have one tested result across every layer.
- [x] Persistence replacement and rollback are adapter-owned, declared tables reject drift, and routes do not access store internals.
- [x] Operation identifiers are registered once and every cross-domain outcome conversion is explicit and tested.
- [x] Media tiers, bitrates, capacity guards, timestamps, IDs, validation, and coded errors each have one appropriate authority.
- [x] Services depend on repositories and one composition root; no production path uses a throw-only or parallel authority.
- [x] Test helpers replace repeated harnesses and fixtures while `node test-all.js` remains green.
- [ ] Live and Playback render through the same operational component/controller family with mode-specific slots and independent state.
- [ ] Browser-level resize, stacking, floating, fullscreen, accessibility, listener-lifecycle, and Playback timeline/segment tests pass.

## Current Prototype Checkpoint - 2026-07-19

This checkpoint records code that exists for evaluation. It does **not** check the P2, P3, or P5 roadmap boxes, because the corresponding production scope, real integrations, and full acceptance criteria remain unapproved.

### Shared Operational UI Scaffold

- [x] The camera stage/grid is the full background surface in Monitor and Playback.
- [x] Workspace tabs, camera/group browser, per-workspace search, selected-camera inspector, and shared toolbars layer over the grid; the primary application rail remains outside the workspace stack.
- [x] Playback keeps start/end date-time inputs, transport, day navigation, speed control, a requested-range footage bar, and a draggable playhead as a bottom mode extension.
- [x] Fixed grids and spatial canvases follow their containers in docked, floating, restored, and narrow-window source-level regression checks.
- [x] Live and Playback can be independently floated and ordered front/back without allowing either workspace to cover the primary rail.
- [ ] Common UI is rendered by one component/controller family rather than parallel Live and Playback implementations. *(The shared shell, resource tree/rows, fixed grid, drawer, and common toolbar/controller paths are built; the spatial-canvas controller, mode-popover lifecycle, and legacy CSS cleanup remain before this check closes.)*
- [ ] A real-browser viewport matrix verifies resize, page-scroll, stacking, fullscreen, float recovery, and hidden-to-visible canvas behavior.
- [ ] Monitor snapshot and recent-event behavior is functional and capability-gated.
- [ ] Playback receives fresh recorder-backed availability segments and distinguishes available, missing, unknown, overlapping, and multi-source footage.
- [ ] Real live/playback media, evidence export, secure credentials, authenticated access, and authoritative production persistence pass their controlling gates.

### Approved Operational Workspace UI Decision

The following bounded UI direction is approved for the shared scaffold and C0-08. It does not approve real media, evidence, device, or persistence behavior:

1. Mount one Live instance and one Playback instance of the same configurable workspace component family. Do not move one literal DOM panel between routes; both workspaces may be visible or floating at once.
2. Within each workspace, layer back-to-front: camera stage/grid; Playback timeline/range extension when applicable; resource browser and selected-camera inspector; shared grid toolbar and workspace tabs; per-workspace search; popovers, menus, and tooltips.
3. The camera stage always fills the usable workspace. Opening an overlay must not reflow or shrink the grid. Overlay panels scroll internally; operational pages do not scroll.
4. Live mode supplies digital zoom, PTZ, snapshot, stream settings, and recent-event extensions. Playback supplies start/end date-time selection, transport, speed/day navigation, availability segments, draggable playhead, and evidence actions.
5. Each instance owns independent query, selection, panel state, grid division, maximized camera, tabs, canvas runtime, and playback range. Shared inventory and resource admission remain common services.
6. Sizing is container-driven with recoverable dock/float/fullscreen transitions. Narrow layouts overlay controls rather than forcing a fixed-width canvas beyond the viewport.
7. Requested time is never labeled as recorded or available footage. Availability, gaps, and source identity require fresh evidence from the selected recorder or recording service.

### Operational Workspace Acceptance Checks

- [ ] At representative desktop and restored-window sizes, document scroll remains absent, stage bounds equal workspace bounds, overlays remain inside the workspace, and the application rail never intersects a workspace.
- [ ] Computed stacking order is stage < Playback timeline < panels < toolbar/tabs/search < popovers, with the active floating workspace above the other.
- [ ] Fixed divisions and both spatial canvases exactly track container size after resize, rail collapse, dock/float, fullscreen, maximize/restore, and hidden-to-visible transitions.
- [ ] Opening the resource browser or inspector does not change stage bounds; both panels remain keyboard reachable and internally scrollable.
- [ ] Live query, selection, panels, grid, and maximize state never mutate Playback state, and the reverse is also true while both are visible.
- [ ] Shared camera-session limits include all active/inactive, docked/floating, and front/back workspaces without double-counting one camera inside one workspace.
- [ ] Playback validates start before end, handles invalid/empty/time-zone cases, supports pointer and keyboard scrubbing, and exposes accessible playhead value text.
- [ ] Availability tests cover continuous/event segments, gaps, overlaps, duplicate sources, source outage, partial-camera results, and unknown state.
- [ ] One handler is registered per action, observers/listeners are cleaned up, inactive work is bounded, and lifecycle tests detect duplicate dispatch.
- [ ] Keyboard focus, screen-reader labels, non-color status cues, reduced motion, loading, empty, unsupported, stale, read-only, and partial-error states are verified.

## Native Client Track (N0)

> The owner approved the fully-native C++ client as the delivered runtime on 2026-07-20 (Approval Log row N0). The committed stack and all technical decisions are recorded in [`native/ARCHITECTURE.md`](native/ARCHITECTURE.md), which is a reference contract subordinate to this file. This track covers the **runtime/shell**; product features still flow through their P-phase gates. Native increments are verified on real hardware before the next begins, and — like inc 1–3 — a measurement spike checks **no** feature box.

### Native Increment Ledger

| Inc | Component | Character | Status |
| :--- | :--- | :--- | :--- |
| 1 | `vms_hwprobe` — D3D11 per-codec HW-decode probe + machine tier | Measurement / toolchain proof | Built + verified (dev box only) |
| 2 | `vms_shell` — Qt6 + GStreamer window | Toolchain proof | Built + verified |
| 3 | `vms_spike` — RTSP → HW decode → D3D11 render + stats | Measurement spike | Built + verified (live camera) |
| — | Self-contained packaging (bundle Qt + GStreamer) | Packaging proof | Proven (ran with cleaned `PATH`) |
| 4a | `vms_grid` — N-tile HW decode → `d3d11compositor` → one window + per-tile stats; camera-free `--test-pattern` fan-out | Measurement spike | Built + verified on live camera (4-tile); **fan-out ramp on both hardware tiers pending** |
| 4b | Decode governor: `vms_governor` decision library/stateful session + `vms_govtest`/CTest + `vms_grid --govern` decode-path integration | **Product feature** | 🟡 In progress — P3-03 scope approved 2026-07-24. Policy core self-check passes (MSVC `/W4`), including deterministic add/remove/focus re-planning and low/high-water hysteresis; startup profile is seeded from the live per-codec D3D probe plus registered GStreamer hardware-decoder availability; auto-probed 64-tile H.265 integration is **verified on hardware** at 3.77 GB on the corrected I420/`d3d12h265dec` path vs a 10.5 GB crash ungoverned. Acceptance #1 met end-to-end. Changing tier plans are now **applied to live media branches** (`vms_grid --sweep`): a runtime focus sweep re-runs the stateful session, `DiffPlans` orders each change release-before-acquire, and the affected grid is reconfigured to the new plan and keeps decoding within budget — verified on hardware on both the auto/dev-box 64-tile and the tight lowend paths (all tiers resume, memory stays bounded, clean exit, acceptance #6 degrade order honored). The live apply currently rebuilds via a whole-grid NULL→PLAYING cycle on each plan change (the reliable path; a seamless per-tile hot-swap does not survive the D3D12 hardware decoder + reused compositor pad and is deferred as an optimization). The governor's tier decisions now **select real per-camera RTSP streams** (`--govern --camera "main;sub"`): Main opens the main stream, Sub/Thumb the sub stream, Paused opens no session; the credential-free `stream selection: t0=main t1=sub …` line is verified locally along with the tier→stream mapping and graceful per-tile connection handling — live-camera decode confirmation is pending a run with the test camera's credentials. This wires operator-provided URLs only; ONVIF profile discovery/negotiation stays a later gate (P2-05). The dev-box cost model was **recalibrated** against the corrected I420 `d3d12h265dec` ramp (2026-07-25, `native/MEASUREMENTS.md`): corrected ceilings ~70 main / ~249 sub (decode-throughput-bound, not memory-bound — the old 10.5 GB "memory wall" was a pre-fix NVDEC artifact), so `DevBoxProfile` decode budget 15→64, sub cost 0.238→0.28, and a VRAM-tiered auto seed; `--govern --profile auto` now plans 25 main + 39 sub for 64 tiles (vs 1 main + 47 sub + 16 thumb) and `vms_govtest` still passes. The **honest per-tile state model** is built: each decision carries a `TileState` (live / degraded / paused-offscreen / paused-capacity), unit-tested and surfaced live in `vms_grid` (e.g. `tile 0 [main/live]`, `tile 5 [thumb/degraded]`); the *visual* rendering of that state is the separate Qt/QML workspace gate (P3-14), whose **slice 1 is now built and verified** (`vms_workspace`: a Qt Quick grid binding the real `GovernorSession`, each tile drawn in its honest state, live focus-sweep re-plan; realizes acceptance #5 on screen). Remaining: live-camera confirmation of RTSP selection, more workspace UI (per-tile toolbar controls, tabs, Playback instance), a seamless (non-reloading) apply, and low-end (i5 software) calibration — the P3-14 slice-2 group (2a gst→Qt Quick bridge, 2b-1 governed-grid video source with real `d3d12h265dec` decode under aligned chrome, 2b-2 lock-step live re-plan on focus sweep, 2b-3 `--profile auto` live-probe seed), slice 3 (interactive click-to-focus + layered selected-tile info panel), slice 4 (layered resource browser + search + toolbar), slice 5 (operator-settable device priority — the second orthogonal axis), slice 6 (runtime layout presets that resize the wall live), slice 7 (operator-set desired media tier — the first orthogonal axis), and slice 8 (two independently-stateful Live/Playback instances via tabs, with a scaffolded Playback transport) are done and verified. |
| 5 (P0-04) | Persistence — embedded SQLite store behind the C0-03 contract (`vms_persist`): declared tables, atomic writes + RAII rollback, versioned forward migrations, typed errors, backup/export; `WorkspaceRepo`; `vms_workspace` persist/restore | **Product feature** | ✅ Built + verified (dev box) — `persist_selfcheck` 29 checks; 5c layout restore end-to-end. Scope approved 2026-07-26. Central DB/sync → P1-10 |
| 6 (P1-03/N0) | Connection broker + credential store — DPAPI `SecretStore` (6a), `CredentialRepo` atomic `credential_ref`↔secret binding (6b), pure standalone `ConnectionBroker` (6c-1), `vms_grid` connect-by-`camera_id` (6c-2) | **Product feature** | ✅ Built + verified (dev box) — `credential_selfcheck` 45 + `broker_selfcheck` 32 checks; all four CTest suites green. **Broker path confirmed against a live camera 2026-07-29** (resolve→materialize→connect→auth end-to-end; honest per-tile `Unauthorized` under placeholder creds); hardware decode with valid creds is the last live check. Scope approved 2026-07-27 |
| 7 (P4-03/P5-02) | Recording backend + Playback footage — optional deployer-located local recording → SQLite segment index → honest availability query (available/missing/unknown/overlapping) → real Playback transport (seek/speed/frame-step) | **Product feature** | 🟡 **In progress** — scope approved 2026-07-29 (Option A), [`native/recording-playback-P4-03-P5-02-proposal.md`](native/recording-playback-P4-03-P5-02-proposal.md). **7a (segment index) + 7b (availability spans) built & verified (dev box):** schema v4 `segments`, `SegmentIndex` (atomic add + validation so unwritten footage is never indexed, overlap listing, oldest-first size/age retention returning trimmed files), and `ComputeAvailability` (honest available/missing-gap/overlapping spans, never presenting requested time as recorded); `vms_rectest`/`recording_selfcheck` 29 checks, all 5 CTest suites green. **7a-media (recording pipeline) also built & verified (dev box):** `vms_record` muxes a source into fixed-duration segment files via `splitmuxsink` and writes every completed segment into the `SegmentIndex` with its wall-clock [start,end], path, codec, and byte size — a 12s `--test-pattern` run produced 4 real `.mp4` files indexed as one contiguous Available span (record→index→availability proven end-to-end, no camera). **7c-1 (Playback data model) built & verified (dev box):** a `PlaybackController` turns the `SegmentIndex` into the honest model the Playback timeline binds to — availability spans over a requested window (available/missing-gap/overlapping as fractions), a draggable playhead that reports footage-vs-gap and resolves the backing recorded file + offset, and transport state (play/pause/speed/frame-step); `vms_workspace --playback-selftest` passes 12 checks. **7c-2 (Playback timeline + transport, bound to real footage) built & verified (dev box):** the QML Playback tab now binds to a recording-backed `PlaybackController` (`--rec-db`) — the timeline draws real availability bands (green available / amber overlapping / dark gaps, requested time never painted as recorded) and a draggable playhead that reports the recorded time and footage-vs-gap; transport controls (⏮ ◀ᖴ ▶/⏸ ᖴ▶ ⏭ + 1×/2×/4×) drive the controller. Verified via an offscreen smoke run (`--smoke-ms`, `QT_QPA_PLATFORM=offscreen`) against a real `vms_record` DB: the scene loads and every binding evaluates with no QML errors. **7c-3 (decoded video in the Playback pane) built & verified (dev box):** a `PlaybackPipeline` (`filesrc`→`decodebin`→`videoconvert`→RGBA→`appsink`→`VideoItem`, real-time paced, seek/rate) decodes the recorded segment at the playhead into a Playback `VideoItem`; the controller's scrub/play/pause/speed drive it, the decoded position pushes the timeline playhead back, and it rolls onto the next segment file at a boundary. An offscreen smoke that seeks to start + plays pulled **85 real decoded frames** from the recorded `.mp4` files (0 before the fix — `vms_record` now stores **absolute** segment paths so the index is location-independent). **Increment 7's standalone scope is complete** (7a index · 7b availability · 7a-media recording · 7c-1/2/3 Playback); only the **real-camera record path** (store original codec via depay through the broker) remains, and it shares the live-camera hardware/credential block. Honors P0-01E (optional local, no mandatory central archive) |
| 8 (P12-03/P0-01F) | System optimization service — in-process health sampler (free RAM/CPU) → governor re-plan + bandwidth budget + honest optimization report; recommend-only OS remediation | **Product feature** | 🟡 **In progress** — scope approved 2026-07-29 (Option A), [`native/optimization-service-P12-03-proposal.md`](native/optimization-service-P12-03-proposal.md). **Core built & verified (dev box):** a Qt-free swappable `SystemHealthProbe` (Windows impl reads live RAM + system CPU load; injectable fake) + an `Optimizer`/`OptimizerSession` that maps live headroom onto the governor's `CapacityProfile` — never above the static ceiling, tightening immediately but deadbanding recovery so the budget can't flap — plus a **bandwidth budget** added as a third governor admission budget (backward-compatible: `0` = unmodeled) and an honest `OptimizationReport` naming the binding budget. `vms_opttest`/`optimize_selfcheck` 16 checks (proactive degrade before exhaustion, recover-without-flap, bandwidth admission, honest attribution, real Windows probe); all 6 CTest suites green. **The live in-app sampler is now wired & verified too:** `vms_workspace` runs a `QTimer` health sampler that adjusts the Live wall's capacity via `WorkspaceController::applyOptimizedProfile` (re-plans under the new budget only when it changes; the report shows in the header). Verified offscreen with an injected `--opt-fake-ram 400`: the running 16-tile wall degraded to `decoding 9/16` under the clamped `mem 240 MB (ram)` budget, while ample real headroom kept all 16 at main — proactive degrade in the live workspace, recovery when it clears. **Remaining:** true active bandwidth probing (the link budget is operator/estimated for now); the OS-level always-on service and node-connectivity failover stay their own (security-reviewed / P1-10) gates |
| 10 (P2-01/02/05) | Device onboarding inventory layer. `DeviceRepo` (`src/persist/`) on the P0-04 store: atomic **onboard** of a recorder/direct camera + its channels + its credential (secret via `SecretStore` under the broker's `credential_ref` scheme) (P2-01); one protected **auto default group** per device (P2-02); **idempotent channel sync** preserving stable camera ids + operator-customized names (P2-05). Standards-correct inventory, **not a protocol mock**. `vms_devtest`/CTest | ✅ built & verified (dev box): `device_selfcheck` green — 26 checks (atomic onboard, auto group, idempotent re-onboard, operator-name-preserving sync with add/remove, and an **onboard→ConnectionBroker end-to-end resolve** — the onboarded camera is immediately connectable). All CTest suites green. Scope approved 2026-07-29 |
| 11 (P2-03/P2-14) | ONVIF **WS-Discovery** — the standards-exact discovery source feeding `DeviceRepo`. `vms_onvif` (`src/onvif/`, Qt6 Core/Network): a pure `BuildProbe` (WS-Discovery 2005/04 SOAP-UDP, `dn:NetworkVideoTransmitter`) + `ParseProbeMatches` (namespace-prefix-agnostic; EndpointReference/Types/Scopes/XAddrs) + `DedupByEndpoint` + `ScopeValue` decode, and a `QUdpSocket` `Discover(timeout)` multicast transport. **Real ONVIF wire format, not a mock.** `vms_onviftest`/CTest | ✅ WS-Discovery core built & verified (dev box): `onvif_selfcheck` green — 16 checks (well-formed standards-exact Probe with correct Action/To/Types, ProbeMatch parse against real Hikvision + different-prefix Axis fixtures, XAddrs + percent-decoded name/hardware scopes, dedup, honest empty on malformed input). All 8 CTest suites green. **Remaining (live/hardware):** run `Discover()` on the LAN to find the test camera |
| 12 (P2-14) | ONVIF **device/media SOAP client** — completes the discover→onboard path. `OnvifClient` (`src/onvif/`): standards-exact WS-Security UsernameToken **PasswordDigest** header + `GetDeviceInformation`/`GetProfiles`/`GetStreamUri` request builders + prefix-agnostic response parsers (profiles → token/name/encoding/resolution; stream URI; device info). `OnvifTransport` (`FetchDevice`, Qt Network) POSTs them to a device and returns info+profiles+RTSP URIs for `DeviceRepo.onboard`. **Real ONVIF wire format.** `vms_onviftest`/CTest | ✅ core built & verified (dev box): the 25-check `onvif_selfcheck` now covers the client — PasswordDigest matches Base64(SHA1(nonce+created+password)) (recomputed independently) and changes with the password, well-formed GetProfiles/GetStreamUri envelopes with the right ONVIF operations/token/RTSP, and GetProfiles/GetStreamUri/GetDeviceInformation parsing against real device fixtures. All 8 CTest suites green. **Remaining (live/hardware):** `FetchDevice()` against the test camera → `DeviceRepo.onboard` end-to-end |
| 13 (P2-13) | Device health & lifecycle. Pure Qt-free `HealthMonitor` (`src/health/`): per-device **separate honest dimensions** (reachability / stream / storage) → derived state (Unknown/Online/Degraded/Offline/Unsupported, honest Unknown until observed); **exception lifecycle** (raise with first-seen time → operator acknowledge (silences, persists) → auto-clear on recovery); **maintenance windows** suppress the alert but not the state; firmware inventory; `needsAttention()`/`shouldNotify()`; injectable clock. `vms_healthtest`/CTest | ✅ built & verified (dev box): `health_selfcheck` green — 22 checks (Unknown-until-observed, dimension independence + state derivation, exception raise→ack→auto-clear, fresh exception on a new fault, maintenance suppression, storage/stream independence, unsupported + firmware). All 9 CTest suites green. Scope approved 2026-07-29. **Surfaced in the Onboarding UI (increment 14).** Remaining: feed it live from the broker's connect outcomes + ONVIF polling, wire health events (P6-08) |
| 14 (P2-01/02/05/13) | **Onboarding UI** — the operator-facing device-management surface (owner item #3, sequenced after ONVIF ✅ + device health ✅). `DeviceController` (`src/workspace/`, Qt) bridges the two pure cores to QML: `DeviceRepo` (atomic onboard / idempotent channel sync / remove over the P0-04 store + DPAPI `SecretStore`) and `HealthMonitor` (honest per-device reach/stream/storage → derived state, exception raise→ack→auto-clear, maintenance). A **Devices tab** in `vms_workspace` lists onboarded devices with their honest health, an onboard form (single-channel direct camera), and per-device acknowledge / maintenance / remove; new `DeviceRepo::listDevices` read accessor. Built like the Playback UI and offscreen-smoke verified. `--devices-selftest` + offscreen `--smoke-ms --devices-demo` | ✅ built & verified (dev box): `--devices-selftest` green — 17 checks (onboard via the controller, credential stored only via the SecretStore under the broker ref, Unknown-until-observed, reported Offline→exception→needs-attention, acknowledge silences but keeps the exception, recovery auto-clears, remove deletes the secret). Offscreen `--smoke-ms --devices-demo` loads the whole scene + Devices tab with **no QML errors/warnings**. All 9 CTest suites green. **Remaining (live/hardware):** feed health from real broker connect outcomes + ONVIF polling (the `reportReach/Stream/Storage` wiring points) and the on-screen windowed view; discovery-as-a-source (P2-03/14) is its own slice |
| 15 (P2-01) | **NVR/hybrid multi-channel onboarding** — the recorder workflow completing P2-01 alongside the direct-camera path. Schema **v5** adds `devices.kind` (`camera`/`nvr`/`dvr`/`hybrid`, a forward-only additive migration); `DeviceController::onboardRecorder` expands a **channel count + per-channel RTSP URL templates** (`{ch}` → the 1-based channel number) into N channels with stable ids and onboards them atomically with the device, default group, and credential. The Devices form gains a kind selector (recorder kinds show channel-count + template fields); cards show kind + channel count. `--devices-selftest` + offscreen `--smoke-ms --devices-demo` | ✅ built & verified (dev box): `--devices-selftest` green at **24 checks** (adds schema v5, recorder onboard, kind persisted, N channels expanded with stable ids, default group reconciled to all N, idempotent re-onboard grows the channel set). Offscreen smoke seeds 5 cameras + 1 NVR (8 channels), no QML errors/warnings. All 9 CTest suites green. **Remaining:** ONVIF `GetProfiles` as the channel source (live-blocked); per-channel disable/remove sync UI |
| 22 (P2-06) | **Rescan a camera from discovery** — the third P2-06 slice. `DeviceController::rescanDevice(id, user, password)` re-fetches a direct camera's ONVIF media profiles from the discovered device matching its address (host) in the last scan, then reconciles its channel via `syncChannels` (allowRemoval=false) — refreshing the stream URLs + re-syncing the stream profile while **preserving the operator's customized channel name and stable id**. Honestly refuses a recorder (channels come from templates) or a device with no matching discovered device / no usable stream. A "Rescan" pill (camera-kind cards) in `DevicesView.qml` uses the Discover panel's credentials. No schema change. `--devices-selftest` + offscreen `--smoke-ms --devices-demo` | ✅ built & verified (dev box): `--devices-selftest` green at **73 checks** (adds: rescan re-fetches + reconciles, keeps the single channel, preserves the operator name, refreshes the main URL from the device, and honestly refuses a recorder). Offscreen smoke clean. All 9 CTest suites green. Owner directive 2026-07-30. With this **P2-06's buildable scope is complete** (rename · remove-confirmation · attach/detach · rescan); only per-channel detach/rename remains, and the true live ONVIF fetch shares the inc 11/12/16 hardware block |
| 21 (P2-06) | **Device attach/detach** — the second P2-06 slice ("detach configured cameras safely"), the device-level sibling of inc 17's channel disable. Schema **v8** adds `devices.disabled` (forward-only additive). `DeviceRepo::setDeviceDisabled` detaches a device — all rows, channels (stable ids + operator names), and credential preserved, but the default group is **emptied** (`reconcileGroup` now returns an empty group for a detached device) so it is excluded from active use; re-attach restores the group to its enabled channels. `listDevices` carries `disabled`. `DeviceController::setDeviceDisabled` invokable + the model carries `disabled`, and `pollHealth` **skips** detached devices (not managed → not probed). `DevicesView.qml` gains an Attach/Detach pill, dims a detached card, and shows a "⏸ detached" badge. `--devices-selftest` + offscreen `--smoke-ms --devices-demo` | ✅ built & verified (dev box): `--devices-selftest` green at **68 checks** (adds: attached group holds channels, detach sets disabled, detached group emptied, detach preserves channels, the poll skips a detached device, re-attach restores the group). Offscreen smoke migrated the DB → v8 and loaded the attach/detach UI with **no QML errors/warnings**. All 9 CTest suites green (`dbtest` pinned to v8). Owner directive 2026-07-30. **Remaining in P2-06:** rescan-from-discovery and per-channel detach/rename — deferred for an explicit scope decision |
| 20 (P2-06) | **Device rename + destructive-action confirmation** — the first, unambiguous slice of P2-06 (rename/rescan/detach/delete). `DeviceRepo::renameDevice` updates the device name and refreshes its default group's display name while **preserving every association** (camera channels + stable ids + operator names, group membership, credential all untouched); honest `NotFound` on a bad id. `DeviceController::renameDevice` invokable; `DevicesView.qml` gains an inline device-name rename (toggle) and a **two-step confirmation gate** on the destructive Remove (was one click) — "Remove → Confirm remove / Cancel". `--devices-selftest` + offscreen `--smoke-ms --devices-demo` | ✅ built & verified (dev box): `--devices-selftest` green at **62 checks** (adds: renameDevice succeeds, name updated, channels preserved, default-group membership preserved, group display-name refreshed, unknown-device honest error). Offscreen smoke loads the rename/confirm UI with **no QML errors/warnings**. All 9 CTest suites green. Proceeding under the owner's standing "build the next thing, as you recommend, standards-compliant" directive (2026-07-30). **Remaining in P2-06:** detach (vs delete) semantics, rescan-from-discovery, and reduced-channel handling beyond `syncChannels` — deferred for an explicit scope decision |
| 19 (P2-13) | **Device-health live feed** — turns the increment-13 `HealthMonitor` from operator-reported to self-updating, the P2-13/14 "feed health from live polling" remainder. An injectable `DeviceHealthProbe` (`src/workspace/HealthProbe.h`) answers "is this device reachable now?"; `MakeTcpHealthProbe` is the live impl (a bounded Qt `QTcpSocket` connect to the device's media port), a fixture fake serves scripted outcomes. `DeviceController` gains `pollHealth()` (probe every device → `reportReachable`: reachable→reach Online, unreachable→reach Offline raising an exception; stream left Unknown since a TCP handshake doesn't prove a decoding stream) + a `healthFeedAvailable` property; `main` drives it on a `QTimer` (`--health-interval`, `--no-health-feed`), off under `--devices-demo` so the scripted demo stands. `DevicesView.qml` shows a live/manual feed badge. `--devices-selftest` + offscreen `--smoke-ms --devices-demo` | ✅ built & verified (dev box): `--devices-selftest` green at **56 checks** (adds: feed available, a new device Unknown until the feed observes it, reachable→Online, unreachable→Offline + needs-attention, recovery→Online when the probe reports reachable again). Offscreen smoke loads the feed badge with **no QML errors/warnings**. All 9 CTest suites green. **Remaining (live/hardware):** confirming the live TCP probe against the real camera on the LAN, and a richer probe (ONVIF poll for firmware, a media open for true stream health) + async/off-thread probing for large inventories |
| 18 (P2-05) | **Channel reorder + stream-profile sync** — finishes P2-05's verb set. Schema **v7** adds `cameras.sort_order` (forward-only additive; listing is `ORDER BY sort_order, id`, so a default 0 keeps the prior by-id order until the operator rearranges). `DeviceRepo` gains `reorderChannels` (writes each id's 1-based position, device-scoped) and `setStreamProfile` (upsert into the existing v2 `stream_profiles` per `(camera_id, tier)`); `listChannels` LEFT JOINs the `main`-tier profile so each channel carries its synced resolution/codec. `DeviceController` exposes `moveChannel(up/down)` (computes the new order and persists it) and `onboardDiscovered` now **syncs the chosen ONVIF profiles' resolution/codec** (via an extended pure `chooseStreams` that also returns the picked profiles). `DevicesView.qml` channel rows gain ▲/▼ reorder and show `1920×1080 H265`. `--devices-selftest` + offscreen `--smoke-ms --devices-demo` | ✅ built & verified (dev box): `--devices-selftest` green at **51 checks** (adds: schema v7, initial order by id, move-down reorders, move-up restores, move-up-at-top no-op, and the discovered channel's main profile synced to 1920×1080 H264 from the fixture). Offscreen smoke migrated the pre-existing v6 DB → v7 and loaded the reorder/profile UI with **no QML errors/warnings**. All 9 CTest suites green (`dbtest` pinned to v7). With this the full P2-05 verb set — add / update / **reorder** / rename / disable / remove + **stream-profile sync from a discovery source** — is built and offscreen-verified; live ONVIF confirmation shares the inc 11/12/16 hardware block |
| 17 (P2-05) | **Per-channel management / sync UI** — completes P2-05's "disable/remove individual channels" remainder by giving the operator a surface over the channel-sync core built in inc 10. Schema **v6** adds `cameras.disabled` (forward-only additive; a disabled channel stays in inventory but leaves the device's default group — excluded from active use). `DeviceRepo` gains `listChannels`, `renameChannel`, `setChannelDisabled`, `removeChannel`, and its `reconcileGroup` now derives group membership from the **enabled** camera rows (so onboard / sync / disable / remove all reconcile correctly). `DeviceController` carries a per-device `channels` list in its model + rename/disable/remove invokables; `DevicesView.qml` gains an expandable "Channels (N)" panel per device (inline rename, enable/disable, remove). `--devices-selftest` + offscreen `--smoke-ms --devices-demo` | ✅ built & verified (dev box): `--devices-selftest` green at **46 checks** (adds: schema v6, channel model lists all 6, group holds 6 enabled, rename preserves the stable id, disable keeps the channel in inventory but drops it from the group 6→5, remove leaves inventory 6→5 and group→4, re-enable restores it to the group). Offscreen smoke migrated the pre-existing v5 DB → v6 and loaded the Devices tab + channel panels with **no QML errors/warnings**. All 9 CTest suites green (`dbtest` pinned to v6; `device_selfcheck` unaffected by the `reconcileGroup` refactor). **Remaining:** channel **reorder** (needs an order column) and stream-profile (resolution/codec) sync from a discovery source |
| 16 (P2-01/P2-03/P2-14) | **ONVIF discovery → onboard UI** — wires the already-built, fixture-verified ONVIF cores (WS-Discovery inc 11 + device/media SOAP client inc 12) into the Devices tab as a **discover→onboard** flow, closing the "discovery as a data source" remainder of P2-01. An injectable `DiscoverySource` (`src/onvif/`) fronts the two live transports so the flow is offscreen-testable: `DeviceController` gains a `discoveredDevices` model + `startDiscovery()`/`onboardDiscovered()`, a **pure `chooseStreams`** mapping (largest H.264/H.265 profile→Main, a smaller→Sub; JPEG/URI-less ignored), stable `deviceIdFromHost`, and **duplicate detection** flagging candidates already in inventory (P2-03). One-click onboard fetches the device's profiles + RTSP URIs and onboards atomically via `DeviceRepo` (kind `camera`, credential only via the `SecretStore`). A "Discover on LAN" panel in `DevicesView.qml`; the live source is used windowed, a fixture-backed fake for `--devices-selftest`/`--devices-demo`. `--devices-selftest` + offscreen `--smoke-ms --devices-demo` | ✅ built & verified (dev box): `--devices-selftest` green at **36 checks** (adds: discovery available, `chooseStreams` main/sub/JPEG-ignored, `deviceIdFromHost`, scan lists 2 candidates, new-vs-onboarded flag, `onboardDiscovered` end-to-end → mapped id + one channel + vendor from ONVIF info + **credential via the SecretStore under the broker ref**, and a re-scan flagging the just-onboarded device as already onboarded). Offscreen smoke seeds 6 devices + runs one discovery pass (2 candidates, one colliding→"onboarded", one new) with **no QML errors/warnings**. All 9 CTest suites green (`onvif_selfcheck` unaffected). **Remaining (live/hardware):** the real UDP `Discover()` + HTTP `FetchDevice()` on the LAN against the test camera (the same block inc 11/12 carry — the live `DiscoverySource` drops in unchanged); multi-channel ONVIF encoders/NVRs as a discovery source, and bulk/activation-state selection (P2-03), are later slices |
| 30 (P1-13/A0) | **Playback transport commands.** Nine stable commands put both recorded Playback (`playback.play/pause/speed/step/seek`) and instant replay (`replay.play/pause/speed/seek`) behind the existing `playback.review` capability, validation, deterministic result, API discovery, and audit. All discrete QML controls use structured `invoke`; drag frames remain a continuous-input exemption and release commits the final seek. Scope: `native/playback-command-P1-13-proposal.md` | ✅ built & verified (dev box): real `PlaybackController` play/pause/speed/step/seek command checks; `--coverage-check` **0 violations** (39 mutators / 7 direct sites, all read-only or continuous); **no deferred-slice exemptions**; command, coverage, Playback, instant, API, all 10 workspace gates, root regressions, native build, and offscreen QML/video smoke green. All 11 component tests pass; the unchanged ONVIF binary has an intermittent aggregate-CTest Windows loader failure but passes focused CTest. **Remaining:** voice leg, transport hardening, roles/auth |
| 31 (P3-15/A0) | **Premises, floor underlay, and camera coverage.** Schema v11 adds canonical site/floor/active-selection tables plus per-workspace-tile facing/FOV. A Qt-free `PremisesRepo`, read-only `PremisesController`, and three command-envelope verbs configure the active place and orientation. Spatial QML renders the optional plan URI (or named blank-floor fallback), active site/floor/timezone, and FOV wedges without changing governor decisions. Scope: `native/premises-spatial-P3-15-proposal.md` | ✅ built & verified (dev box): persistence reopen + default/custom round-trips; spatial model/restart orientation round-trip; real command-path checks; coverage 0 violations; offscreen schema-v11 spatial smoke clean; root regressions green. Aggregate CTest 10/11 due the unchanged intermittent ONVIF loader issue; focused ONVIF passes. **Remaining P3-15:** measured spatial performance and live-video-vs-map-tile media decision; broader premises hierarchy/editor is separately scoped. |
| 32 (P3-15) | **Measured spatial performance acceptance.** `--spatial-benchmark [N]` and `scripts/spatial-performance.ps1` exercise the shipped 64-tile zone→governor→QVariant-model path, separately measuring identical idle passes and settled pans that force replans. CSV/human output, behavioral invariants, p95 limits, and non-zero failure make it a repeatable gate. Culled cameras no longer paint FOV canvases. Scope: `native/spatial-performance-P3-15-proposal.md` | ✅ built & verified (dev box): Debug idle/active p95 **0.0026/1.9852 ms** (2,000 active replans); Release **0.0006/0.1995 ms** (5,000 replans), within 0.50/4.00 ms limits. Release 64-tile QML load **105 ms**, offscreen smoke clean. All 11 workspace gates + root regressions pass; aggregate native 10/11 only because unchanged ONVIF loader flake, focused pass. Results: `native/MEASUREMENTS.md`. **Remaining P3-15:** the product/media decision for map pins versus live-video spatial tiles. |
| 29 (P1-13/P1-09) | **Command coverage — no UI bypass.** `CommandController::invoke(id,args,confirm)` structured path + 12 new device/channel/workspace commands; **all 27 direct controller call sites in the shipped QML rerouted** through the envelope with no fallback. Pure `CoverageCheck` scans the **shipped** QML (Qt resource) against a declared 39-mutator table and **exits non-zero on a bypass** (`--coverage-check`); exemptions declared, categorized, printed. **P1-09 fix found by this work:** `ParamSpec::secret` + name backstop redact credentials to `***` in audit rows/logs/catalog while handlers still receive the real value. Scope: `native/command-coverage-P1-13-proposal.md` | ✅ built & verified (dev box): `--coverage-check` **0 violations** (2 files / 39 mutators / 18 sites, all declared exemptions); `--coverage-selftest` **10 checks** incl. the negative test (injected bypass detected at the exact line); `command_selfcheck` **37 checks** (adds secret redaction + catalog `"secret":true` + unknown-command backstop). Leak closed end-to-end (raw byte-scan of the audit DB finds no plaintext secret; onboard still succeeded). All 11 CTest + 10 workspace gates green. **Remaining after inc 29:** voice leg, transport hardening, roles (Playback transport closed by inc 30) |
| 28 (P1-06) | **Durable audit history** — schema **v10** `audit_log` + Qt-free `AuditRepo` (`vms_persist`): append-only by contract (no update/delete API), every row hash-chained (`SHA-256(prev_hash ‖ fields)`, injected hash) so any edit/delete breaks `verifyChain` at that row (tamper evidence). The envelope's sink appends durably — every attempt, **palette/api/ui attributed, refusals included**. `--audit-dump [N]` reads rows + chain verdict. `--audit-selftest`. Scope: `native/audit-log-P1-06-proposal.md` | ✅ built & verified (dev box): `--audit-selftest` **18 checks** (durable + reopen, attribution, chain genesis/links, tamper detected at the exact forged row, post-tamper appends keep the break evident). **End-to-end:** API-driven run → exit → `--audit-dump` read back executed + refused attempts, chain intact. All 11 CTest + 8 workspace selftests green. **Remaining:** login/config/export auditing, trusted external timestamps, retention, chain-head anchoring |
| 27 (P6-03/P6-07) | **Event rules & alarm lifecycle** — pure `AlarmEngine` (`src/events/`, lib `vms_events`): events → rules (type + device filter, priority, auto-clear event) → deduplicated accountable alarms (new→ack (persists)→escalated→cleared; fresh-after-clear); maintenance suppresses notification never state; **notifies, never actuates** (P6-05 own gate). First source: `DeviceController::healthTransition` (transitions only) under 3 default rules. 🔔 Alarms chip/panel; `alarm.ack/escalate/clear` in the command envelope (panel = first UI surface routed through commands). `vms_eventtest`/CTest + `--alarms-selftest`. Scope: `native/events-alarms-P6-proposal.md` | ✅ built & verified (dev box): `event_selfcheck` **30 checks**; `--alarms-selftest` **17 checks** end-to-end (real signal path: outage→High alarm→envelope ack→recovery auto-clear→fresh alarm→maintenance suppression). All **11** CTest suites + 7 workspace selftests green; offscreen smoke clean. **Remaining:** P6-05 linkage, P6-04 durable pipeline, rule schedules/correlation, delivery channels/SLO, non-health event sources |
| 26 (P1-13/A0) | **External control API** — `CommandServer` (Qt HttpServer): loopback-only, off-by-default (`--api-port`), bearer-token-authenticated HTTP/JSON in front of the increment-25 envelope. `GET /v1/commands` (catalog discovery) + `POST /v1/invoke` through the **same** gate as the palette (validation/capability/dangerous-confirm/audit), deterministic status mapping (200/404/400/403/409/500). `--api-selftest`. Scope: `native/command-api-P1-13-proposal.md` | ✅ built & verified (dev box): `--api-selftest` green — **16 checks**; **end-to-end proof**: an external client discovered 11 commands and drove the running app (layout + focus) over HTTP, 409 on unconfirmed `device.remove`, 401 without the token, all in the shared audit trail. All 10 CTest suites + 5 workspace selftests green. **Remaining:** scoped tokens/TLS/rate limits/streaming, non-loopback exposure (security-reviewed), the agent connector, the UI-coverage check |
| 25 (P1-12/A0) | **Unified command envelope** — the A0 mandate's first slice. Pure `CommandRegistry` (`src/command/`, lib `vms_command`): typed `CommandSpec` catalog; one gate per invocation (validate → capability → dangerous confirm → execute → deterministic result) with **every attempt audited, refusals included**; deterministic text leg (`"focus 5"`, trailing `confirm`); machine-readable JSON catalog (`--commands`, the P1-13 seed). `CommandController` registers the native verbs over the live controllers + a **⌘ Command palette** (Ctrl+K). `vms_cmdtest`/CTest + `--command-selftest`. Scope: `native/command-envelope-P1-12-proposal.md` | ✅ built & verified (dev box): `command_selfcheck` green — **32 checks**; `--command-selftest` green — **18 checks** (text drives the real workspace; honest failures; needs-confirm; capability-denied; spatial round-trip; newest-first audit). All **10** CTest suites green; all workspace regressions green; offscreen smoke clean. **Remaining:** external API + coverage check (P1-13), voice (P1-12), durable audit (P1-06), RBAC (P1-02/03), full QML-click rewiring (P1-13 acceptance) |
| 24 (P3-15/P3-01) | **Spatial camera canvas** — the prototype's `spatial-canvas.js` rebuilt natively "same or better" (owner directive 2026-07-31). Grid \| Spatial toggle on the Live tab; pure `SpatialPolicy.h` (prototype-exact zones/zoom-levels/tier-caps/anti-flap dwell); `WorkspaceController::updateSpatialViewport` drives the real governor in ONE batched re-plan (culled → `visible=false` → PausedOffscreen, truly not decoding); per-tile **drag repositions** (P3-01 drag verb) + canvas pan + wheel zoom-to-cursor (0.15–6×) + fit; schema **v9** persists positions (`pos_x/pos_y`); map-pin dots at site zoom; prototype tier labels (Idle/Preview/Live·SD/Live·HD) beside the honest P3-14 state. `--spatial` + `--spatial-selftest`. Scope: `native/spatial-canvas-P3-15-proposal.md` | ✅ built & verified (dev box): `--spatial-selftest` green — **33 checks** (policy parity incl. focused-bypasses-cap, dwell both ways, drag-never-replans, one-replan-per-pass, unchanged-pass free, honest culling, site-zoom immediate downgrade, v9 position save/restore). `persist_selfcheck` extended (v9 + round-trip); all 9 CTest suites green; playback/instant/devices regressions green; offscreen `--spatial` smoke clean, **real v8 DB migrated → v9 cleanly**. **Remaining:** live video under the spatial tiles (later media slice), floor-plan underlays/FOV/premises entity (rest of P3-15), named layouts (rest of P3-01) |
| 23 (P3-05) | **Instant playback from live view** — the "instant replay" verb. `InstantReplayController` (`src/workspace/`, Qt + `vms_persist`) **owns** a `PlaybackController` (inc 7c-1, no duplication) pinned to a look-back window and drives a dedicated `PlaybackPipeline` (inc 7c-3) → a third `instantVideoOut`. `replay(seconds)` computes the camera's **live edge** (latest recorded moment), opens `[edge − seconds, edge]`, seeks the playhead to the window start, and auto-plays; `returnToLive()` closes + pauses. Honest per P0-03: no local recording → overlay opens **unavailable** (no fabricated spans, requested time never shown as recorded); a look-back longer than the footage **clamps** to the earliest recorded moment and reports the *actual* look-back. UI: a "10s/30s/60s" control on the Live info panel + a layered replay overlay over the still-running live wall (P3-14 layering) with a compact transport + "⟵ Live". `Q_INVOKABLE` (A0-ready). Read-only over the **local** recording index (P0-01E). Scope: `native/instant-playback-P3-05-proposal.md`. `--instant-selftest` + offscreen `--smoke-ms --rec-db` | ✅ built & verified (dev box): `--instant-selftest` green — **22 checks** (starts inactive; live edge = latest recorded; replay(30) window ends at the edge / starts 30s before / playhead ON footage / auto-plays / a file backs the playhead; replay(600) clamps to earliest + reports 90s honestly; return-to-live closes + pauses; unrecorded camera honestly unavailable, no fabricated spans). Offscreen smoke against a real `vms_record` DB: replay overlay binds with **no QML errors/warnings**, instant decode pulled **89 real frames** (look-back honestly clamped to the ~20s that exists). `--playback-selftest` (12) + `--devices-selftest` (75) regressions green. Owner selected the lane 2026-07-31; built under the standing directive. **Remaining:** arbitrary live tile → `camera_id` mapping (later media gate — this pass replays the `--rec-camera`), recorder/NVR/cloud instant retrieval, synchronized multi-camera instant replay (P5-01), and RBAC/evidence gating |

| 33 (P3-15) | **Adaptive spatial live tiles.** Progressive pins → state cards → governed live imagery by zoom. Room tiles crop the one existing `GridPipeline` composite through shared-frame `VideoItem` consumers; only honest live/degraded tiles allocate them, with state/FOV chrome retained. | **Product feature** | ✅ Built + verified (dev box) — Release 64-tile low-end smoke: 16 eligible/consumers/receiving, 223 delivered frames, 16 unique crops, 48 delegates without a consumer, QML load 85 ms. Uneven cell math self-tested; performance gate unchanged at 0.0006/0.1874 ms p95 Release; all workspace/root regressions green. Synthetic-media evidence only; real-camera and physical low-end/display checks remain hardware gates. Scope `native/spatial-live-tiles-P3-15-proposal.md`. |

| 34 (P3-04) | **Honest live diagnostics.** Stable per-branch decoded-buffer probes derive playing/connecting/stalled/paused; the displayed FPS is measured at compositor→Qt delivery so decoder-ahead traffic cannot inflate it. A separate tile-id-indexed diagnostics model exposes active codec/resolution/FPS and explicit unavailable reasons for bitrate/latency/packet loss; the selected-camera overlay renders it. | **Product feature** | ✅ First slice built + verified (dev box) — Release 64-tile low-end smoke: 16 active/playing/FPS/media-known, 48/48 paused honest, 29.7 FPS, 92 ms QML; `--diagnostics-selftest` green; all 12 workspace gates/root regressions pass. Separate diagnostics signaling preserves the 64-tile hot path at 0.0006/0.1691 ms p95 Release. Real-camera transport telemetry remains source-blocked. Scope `native/live-diagnostics-P3-04-proposal.md`. |

| 35 (P3-18) | **Premises operations panel foundation.** `SiteOperationsController` aggregates the active persisted site/floor/timezone, valid site-local clock, device health/lifecycle counts, and governed Live media diagnostics into a read-only model. A toggleable front-layer panel renders those values and explicit reasoned unavailable states for schedules/open status, uptime/last-seen, cumulative duration, and analytics. | **Product feature** | ✅ First slice built + verified (dev box) — `--site-operations-selftest` 15/15; Release persistent video smoke loaded QML in 103 ms with 6 source-accurate devices, 4 playing streams, and 24.6 FPS. Spatial performance remains 0.0010/0.2185 ms p95 Release; all 13 workspace gates/root regressions pass; focused ONVIF passes despite the unchanged aggregate-loader flake. Scope `native/premises-operations-P3-18-proposal.md`. |

| 36 (P3-18) | **Operating hours and open state.** Schema v12 adds a site schedule marker, split weekly local-time windows, closed/special-hours date exceptions, and cascade ownership. `PremisesRepo` persists them atomically; five `premises.hours.*` commands configure them through the audited envelope. UTC instants are evaluated through the site's IANA timezone and the front-layer panel renders Open/Closed, today's hours, and the governing rule. | **Product feature** | ✅ Second slice built + verified (dev box) — persistence/reopen/overlap gates green; `--site-operations-selftest` 23/23 includes half-open boundaries, overrides, spring-forward, and both repeated fall-back hours. An authenticated API mutation reached the running panel; Release persistent video smoke loaded QML in 97 ms with `hours=Open`, panel loaded, 4 playing streams, and 24.0 FPS. Performance remains 0.0006/0.1680 ms p95 Release; all workspace/root regressions green. Scope `native/operating-hours-P3-18-proposal.md`. |

| 37 (P3-18) | **Device-site ownership and reachability time.** Schema v13 adds optional canonical device→site ownership and monotonic persisted reach observations (`observed`, last positive seen, current positive-run start). `device.site` uses the audited envelope; the device model preserves restart honesty; site operations excludes other-site/unassigned inventory and renders current evidenced uptime plus latest seen. | **Product feature** | ✅ Third slice built + verified (dev box) — persistence 49/49, Devices 85/85, site operations 26/26. Release persistent video smoke: schema v13, 92 ms QML, 6 assigned devices, 4 current reachable runs, latest-seen persisted, 4 playing streams, 26.4 FPS. Performance remains 0.0006/0.1322 ms p95 Release; all workspace/root regressions green. Scope `native/device-site-uptime-P3-18-proposal.md`. |

| 38 (P3-18) | **Cumulative completed-recording duration.** `SegmentIndex` validates canonical UTC and unions completed intervals per camera before summing camera-time. The active premises passes only its currently owned stable channel ids; unassigned/other-site/orphan footage is excluded. The panel shows duration and evidence counts while persistent streaming duration remains reasoned unavailable. | **Product feature** | ✅ Fourth slice built + verified (dev box) — recording duration/validation gates green; site operations 29/29. Release synthetic record→index→panel smoke reported 5 seconds / 2 completed segments, QML load 91 ms, 4 playing streams, and 26.1 FPS. Performance remains 0.0006/0.1120 ms p95 Release; all workspace/root regressions green. Scope `native/recording-duration-P3-18-proposal.md`. |

| 39 (P3-18) | **Persistent decoded streaming duration.** Schema v14 stores premises-scoped observed `playing` branch-time in milliseconds. A Qt-free repository adds monotonic elapsed × playing branches atomically with overflow refusal; the controller settles the prior premises before a site switch and never fills process downtime. The panel keeps recording and streaming evidence separate and combines them only when both are available. | **Product feature** | ✅ Fifth slice built + verified (dev box) — persistence 54/54; site operations 30/30. Two Release persistent video runs advanced 18,708→29,248 ms across reopen, with four playing branches, 93 ms QML load, and 25.6 FPS on the second run. Release p95 remains 0.0006/0.1377 ms; all 13 workspace gates/root regressions green; focused ONVIF passes despite the unchanged aggregate loader flake. Scope `native/streaming-duration-P3-18-proposal.md`. |

| 40 (P3-18) | **Site-scoped active alarm analysis.** The panel joins the current active device-health alarm read model to current device→site ownership and reports attention, severity, acknowledgement, suppression, occurrence, and affected-device counts. It is explicitly current-session evidence, not VCA or retained history; site switches exclude other-site and unassigned alarms. | **Product feature** | ✅ Sixth slice built + verified (dev box) — site operations 32/32; Release offscreen QML smoke 160 ms; Release p95 0.0009/0.1717 ms; all 13 workspace gates and root regressions pass; focused ONVIF passes despite the unchanged aggregate loader flake. This completes P3-18's approved standalone scope. Scope `native/alarm-analysis-P3-18-proposal.md`. |

| 41 (P3-14) | **Continuity-preserving same-layout live re-plan.** A plan change briefly pauses the graph while the last Qt frame remains visible, replaces only changed tiles with fresh decode/queue/upload/compositor-pad branches, preserves unchanged decoders/pads, then resumes. Full NULL rebuild is a counted fallback; layout changes still rebuild geometry. | **Media optimization** | ✅ Built + verified (dev box) — Release 64-tile H.265/D3D12 stress completed 11 applies / 23 branch replacements / 0 full fallbacks; subsequent applies 25.2–70.8 ms, 16 playing at 29.2 FPS, 46/46 paused honest. All workspace/root regressions green; focused ONVIF passes despite the unchanged aggregate loader flake. Scope `native/seamless-live-replan-P3-14-proposal.md`. |
| 42 (P1-13/A0) | **External control API rate limiting.** One fixed 60-second request budget now covers catalog, invoke, unauthorized, malformed, and unknown-route traffic. The secure default is 120 requests and `--api-rate-limit N` permits a positive deployment-local override; exhaustion returns structured HTTP 429 before command execution. | **Security/transport hardening** | ✅ Built + verified (dev box) — `--api-selftest` 21/21; a 2-request fixture admits two calls, rejects the third, and proves unchanged workspace state. Non-positive CLI budget exits 2; all 13 workspace gates/root regressions pass; Release p95 0.0006/0.1136 ms; focused ONVIF passes despite the unchanged aggregate loader flake. Scope `native/api-rate-limit-P1-13-proposal.md`. |

### Native Track Outstanding Work

- **Empirical data:** the corrected I420 `vms_grid --test-pattern` main/sub ramps on the dev box are **done** (2026-07-25, `native/MEASUREMENTS.md`): corrected ceilings ~70 main / ~249 sub, decode-throughput-bound (RSS low, no memory crash), and the governor cost model was recalibrated (`DevBoxProfile` 15→64, sub cost 0.238→0.28, VRAM-tiered auto seed). A dev-box **H.264** `--test-pattern` ramp (2026-07-29, 2→16 tiles) showed a flat ~800 aggregate fps / ~32 realtime-stream throughput ceiling at 0% CPU (GPU-bound), a useful supplementary datapoint. **Still remaining:** the same ramp plus `vms_hwprobe.exe` on the **actual** low-end **i5 4th-gen / 8 GB / no-discrete-GPU** machine — a 2026-07-29 ramp labeled `lowend-i5` was in fact run on the dev box (i7-10700 / RTX 3050, 0% CPU = GPU decode), so it does **not** calibrate `LowEndProfile()`, which stays a placeholder until the real i5 box is measured. That is the remaining calibration constraint for the approved governor.
- **Active feature step:** applying the stateful session's changing tier plans to active media branches is **built and verified on hardware** (`vms_grid --sweep`, whole-grid NULL→rebuild on each plan change); the governor's tier decisions **select real per-camera RTSP main/sub streams** (`--govern --camera "main;sub"`, plumbing verified locally; live-camera decode pending a credentialed run); the **honest per-tile state model** (live/degraded/paused-offscreen/paused-capacity) is built, unit-tested, and surfaced live in the harness; and its **visual rendering (P3-14 slice 1, `vms_workspace`) is now built and verified on the dev box** — a Qt Quick grid binding the real `GovernorSession`, each tile drawn in its honest state with an aggregate capacity meter and over-capacity warning, re-planning on a live focus sweep (Qt6 Quick only, no GStreamer/D3D; `--selftest` + windowed). This realizes P3-03 acceptance #5 on screen. **P3-14 slices 2a and 2b-1 are also built and verified (2026-07-26):** the gst→Qt Quick video bridge (a `VideoItem`/`QQuickItem` fed by a GStreamer `appsink`, `appsink → QImage → QSGTexture`; `qml6glsink` is absent from the MSVC binaries so this interop-free route was chosen), and then the **governed d3d11-composited grid as the real video source** (`GridPipeline`: per-tile hardware decode → `d3d11compositor` → `d3d11download` → `appsink` → `VideoItem`, built from the governor's initial plan, chrome aligned cell-for-cell, paused tiles black). Verified with `vms_workspace --video`: 16 tiles decode real H.265 (`d3d12h265dec`, hardware) under the honest chrome, a focus sweep re-plans **both** chrome and live video in lock-step (slice 2b-2: `GridPipeline::applyPlan` whole-graph NULL→rebuild-fronts→PLAYING, the MAIN tile moves across the grid), and **`--profile auto` (now the default) seeds the governor from the live hardware probe** (slice 2b-3: RTX 3050 → 25 main + 39 sub of 64, matching `vms_grid`) — all verified on the dev box, completing the P3-14 slice-2 group. Next native-verifiable step: a **seamless (non-reloading) live apply** (per-tile hot-swap instead of whole-graph rebuild); then (blocked on hardware) live-camera RTSP confirmation and low-end i5 calibration.
- **Increment 41 media update:** the seamless/non-reloading same-layout apply is now built and verified. The graph retains the last complete Qt frame while PAUSED, replaces only changed branches with fresh compositor pads, preserves unchanged decoders, and resumes without a full-NULL fallback in repeated H.265/D3D12 stress. Layout geometry changes still rebuild the grid. Valid-credential camera confirmation and real low-end i5 calibration remain hardware evidence gates.
- **Codec-licensing gate:** H.264/H.265 patent licensing for a distributed product remains a P0-14/P0-17 gate; preferring OS/GPU decoders sidesteps it but it is not cleared. Do not distribute before it is resolved.

## Next Decision and Delivery Lanes

There is one controlling order with parallel work only where it does not pre-empt an unapproved product decision:

1. **Delivered-runtime lane (native, N0):** the active build track. P3-03 is approved and increment 4b is in progress; applying stateful tier changes to active media branches and selecting real per-camera RTSP main/sub streams by tier are now built (RTSP plumbing verified locally; live-camera decode pending a credentialed run), honest Qt/QML per-tile state is now built and verified (P3-14 slice 1, `vms_workspace`), the gst→Qt Quick video bridge is proven (slice 2a), the governed d3d11-composited grid now renders as real hardware-decoded video under the aligned chrome (slice 2b-1, `--video`), a focus sweep re-plans both chrome and live video in lock-step (slice 2b-2), `--profile auto` seeds the governor from the live hardware probe (slice 2b-3), clicking a tile now drives the working set (slice 3: `focusTile` protects the clicked tile at Main, chrome + live video + a layered info panel all follow), a layered resource browser (camera list + search) with a toolbar layers over the grid (slice 4), operator-settable device priority exercises the governor's second orthogonal axis (slice 5: persistent priority, degrade tie-breaker under pressure), runtime layout presets resize the wall live (slice 6: `setTileCount`, video pipeline rebuilt with cached clips), operator-set desired media tier makes the first orthogonal axis interactive (slice 7: Main/Sub/Thumb/Off caps free budget), and the two independently-stateful Live/Playback instances are now built (slice 8: tabbed, `governor` binds the active instance, Playback transport scaffolded pending recordings) — the Phase-4 local recording/index/playback path, real decoded Playback transport, instant replay, and increment-30 command-routed transport are now built and verified. The next native steps require either hardware evidence (valid-credential live-camera decode and the real low-end i5 calibration) or a newly agreed scope: the premises/site model and remaining P3-15 floor-plan/FOV work are the recommended product decision because P3-18 depends on them. The seamless (non-reloading) live apply remains a contained media optimization. See the Native Client Track section above.
2. **Next product decision:** ~~P0-04 authoritative data and persistence~~ **decided 2026-07-26** — the owner selected the embedded, standalone-first direction (SQLite as the local authority behind the C0-03 adapter contract; central/cloud/sync deferred to P1-10). **built and verified as native increment 5** per `native/persistence-P0-04-proposal.md` (store + migrations + layout persist/restore; central/sync deferred to P1-10). The subsequent gates queued by the owner are the connection broker + credential store (DPAPI), then the Phase-4 recording backend. **The connection broker + credential store (DPAPI) gate is now scope-approved (2026-07-27, Option A — in-process broker + Windows DPAPI) and native increments 6a (the `SecretStore` interface + DPAPI backend + `vms_credtest`/CTest) and 6b (the `CredentialRepo` atomic `credential_ref`↔secret binding; the DB holds only the opaque ref, provisioning is both-or-neither, `credential_selfcheck` now 45 checks), and 6c-1 (the pure standalone `ConnectionBroker` — connect-by-`camera_id`, in-memory credentialed-URL materialization, per-device pooling, P0-03 backoff/circuit-breaker, honest typed failures; `broker_selfcheck` 32 checks), and 6c-2 (`vms_grid` connects by `camera_id` through the broker via `--broker-db`/`--provision`; `vms_workspace` has no URL-with-password path yet — synthetic clips; `--broker-selftest` verified) are built and verified on the dev box** (`native/credential-broker-P1-03-proposal.md`), completing the broker gate's standalone scope (6a+6b+6c). The broker path was **confirmed against a live camera on 2026-07-29** (resolve→materialize→connect→auth end-to-end, honest per-tile `Unauthorized` under placeholder creds); hardware decode with valid creds is the last live check. **The owner set the delivery order on 2026-07-29:** (1) live-camera decode confirmation + real low-end i5 calibration (both operator-run on the respective hardware), then (2) the **Phase-4 recording backend + Playback footage** — scope **proposed** in [`native/recording-playback-P4-03-P5-02-proposal.md`](native/recording-playback-P4-03-P5-02-proposal.md) (optional local recording → segment index → honest availability → real Playback transport; awaiting approval; native increment 7), then (3) the **system optimization service** — scope **proposed** in [`native/optimization-service-P12-03-proposal.md`](native/optimization-service-P12-03-proposal.md) (native increment 8, resequenced to last so real live+playback load makes the bottleneck signals meaningful). A new owner requirement, **P3-18** (front-layer per-entity operations time + analysis + device panel), is recorded and awaits a scope discussion. The **seamless (non-reloading) live tier apply** remains a deferred optimization (blocked by the D3D12 decoder + reused compositor pad).
**Lane update (2026-08-09):** increment 41 supersedes the stale seamless-apply remainder in lanes 1–2 above. Same-layout changed-branch replacement is built and verified; the remaining native constraints are valid-credential camera evidence, real low-end i5 calibration, and newly agreed product scope.

**Command/API update (2026-08-09):** increment 42 closes P1-13's standalone request-rate-limit remainder with a default/configurable fixed-window budget and deterministic 429 backpressure. Scoped identities, TLS, streaming, remote exposure, and the AI-agent connector still require their coordinated security/product scopes.

3. **Reference-harness consolidation (C0-08):** the owner explicitly restored this lane to active priority on 2026-08-09. C0-02 through C0-07 are built and verified; C0-08 is in progress. Continue the remaining browser/Node reference-harness consolidation without changing N0's delivered-runtime decision.
4. **Frontend prototype lane:** the historical browser-screen sequence (Devices, Events/Incidents, Reports/Compliance/Tickets, then Settings/Map/Access polish) now serves the reference harness. A prototype screen does not check its P-phase feature boxes.

| Frontend slice | Current state | Owning roadmap items |
| :--- | :--- | :--- |
| Application shell | Prototype scaffold present | P3-14 / C0-08 |
| Monitor | Layered scaffold present; snapshot/recent events and real media remain incomplete | P3-01 through P3-17 |
| Playback | Layered scaffold and requested-range timeline present; real availability/media/evidence remain incomplete | P5-01 through P5-10 |
| Devices | Existing prototype inventory screen; redesign and real onboarding/integration remain unapproved | P2-01 through P2-14 |
| Events/Incidents | Existing prototype elements; production event/alarm lifecycle remains unapproved | P5-08/P5-09 / P6-01 through P6-13 |
| Reports/Compliance/Tickets | Existing report/compliance stubs and services; dedicated workflow remains unapproved | P1-11/P1-12 / P5-10 / P6-11 through P6-13 |
| Settings/Map/Access | Prototype surfaces only; production capability, map, identity, and access scope remain unapproved | P0-05 through P0-17 / P1 / P7 / P8 |

## Phase 0 - Product Direction, Evidence, and Capacity Gates

- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **P0-01 [Architecture/Benefit] Product and deployment goal.** The project owner explicitly approved the complete P0-01 direction on 2026-07-15. The accepted contract, documentation alignment, and Administrator-only permission-ready prototype baseline are implemented and verified. This completion does not authorize implementation belonging to later roadmap gates.

### P0-01 Approved Decision Record

> These entries are approved as the product direction. They authorize the P0-01 architecture contract only. Detailed feature and infrastructure implementation remains subject to its own later roadmap gate.

| Sub-item | Topic | Current state | Approved decision |
| :--- | :--- | :--- | :--- |
| P0-01A | First users | Accepted | Release one starts with one **Administrator** role having all available privileges. Features must be designed around stable capabilities/permission boundaries so later role separation does not require rewriting them. The role editor and feature-to-role assignment will be built after the initial feature set; strong role-based permissions remain a later requirement under P1-02/P1-03. |
| P0-01B | Site pattern and scale | Accepted | The intended architecture supports many local and remote control rooms, sites, and operators, with an eventual scale direction of hundreds of thousands of registered cameras and thousands of NVRs/DVRs. The software must not impose artificial commercial limits. Measurable release profiles and horizontal-scaling boundaries will be defined and tested later; "any number" is not an untested capacity guarantee. |
| P0-01C | Primary topology | Accepted | **Hybrid** means self-contained standalone local operation plus optional self-hosted coordinated and scale-out operation for shared multi-site/control-room use. It does not imply a required proprietary cloud subscription. Exact service placement, synchronization, and failure behavior remain later gates. |
| P0-01D | Cost and licensing | Accepted | The product is intended to be open source and must not require a premium vendor plan, mandatory proprietary cloud, recurring vendor subscription, or forced telemetry. The source research is a feature-discovery input, not a dependency or licensing plan. The repository's specific open-source license remains a later owner decision. |
| P0-01E | Video-recording ownership | Accepted | The VMS will not normally ingest every camera continuously into its own central archive. NVRs, DVRs, devices, or user-selected storage remain authoritative for recordings. The VMS opens live streams when needed and requests recorded footage for playback/review or download on demand through supported device links/protocols. Downloaded evidence scope is handled separately under P5-08/P5-09. |
| P0-01F | Enterprise architecture | Accepted at product-direction level | Optional self-hosted coordination stores shared VMS metadata, not continuous footage. Workers/site agents, load balancing, background processing, capacity-aware optimization, device activity priorities, and an optimization report may be added as scale requires. Exact database, service, high-availability, backup, auditing, and monitoring designs remain later gates. |
| P0-01G | Data control | Accepted | The deployer controls where VMS metadata, logs, settings, credentials, and downloaded evidence are stored. The core has no forced cloud, telemetry, or country-specific hosting assumption. Country-specific legal deployment remains the deployer's responsibility. |
| P0-01H | Latency | Accepted; measurements deferred | Build functional device-management and media paths first using verifiable VMS-like connection/stream-selection/reconnect behavior. Establish numeric defaults through protocol/device research and tests against working software, then optimize iteratively and validate before production readiness. |
| P0-01I | Distributed-site operations | Accepted as roadmap scope | The roadmap includes centralized site/device visibility, site-scoped permissions, remote health monitoring, cross-site live view/playback, time-zone handling, WAN/cloud outage behavior, central alarm escalation, and per-site retention/security configuration. Each capability still requires its own later approval gate. |
| P0-01J | Local infrastructure | Accepted | Small installations require minimal mandatory infrastructure. Larger installations add optional self-hosted coordination, databases, workers, and site agents as needed; they do not depend on a paid vendor cloud. Cameras, recorders, networks, storage, and operator machines remain deployment requirements. |
| P0-01K | Release-one priority order | Accepted | Prioritize scalable device orchestration, device manipulation, and compliance workflows without centrally recording every 24/7 stream. Build working behavior before latency tuning. Use activity/priority states so only the necessary subset of devices maintains expensive streaming or high-frequency work. Exact conflict rules and resource budgets remain later design/test gates. |
| P0-01L | Device activity and priority model | Accepted as a concept | Devices/connections will have resource states such as **high**, **medium**, **low**, and **idle**. High-priority devices may stream or receive frequent processing; idle devices must not consume continuous streaming resources. State meanings, automatic/manual transitions, alarms that can elevate priority, fairness, and per-host limits will be discussed before this feature is implemented. This is separate from per-view media-quality tiers such as main/sub/thumb/paused. |

### P0-01 Accepted Architecture Contract

The following statements are normative for future development:

- The VMS **must** work as a self-contained standalone installation for small deployments.
- The VMS **must** be able to grow through optional, self-hosted coordination, shared metadata services, workers, and site agents; no managed vendor cloud is mandatory.
- Continuous recordings **must** remain owned by NVRs, DVRs, edge devices, or storage selected by the deployer unless a later, separately approved recording feature explicitly changes that behavior.
- The VMS **must** use live streams transiently and request recorder playback/downloads on demand; it **must not** create an implicit central 24/7 archive.
- The initial application role is Administrator, but feature boundaries **must** remain permission-ready for later RBAC.
- The product **must not** impose artificial commercial limits. Published limits must be measurable technical/deployment profiles rather than unlimited claims.
- The deployer **must** control VMS data placement. The core **must not** require forced telemetry, a proprietary cloud, or a vendor subscription.
- Device activity priority and media quality are separate controls. High/medium/low/idle manages connection/work cost; main/sub/thumb/paused manages a selected view's media quality.

#### Deployment Profiles

| Profile | Required components | Intended use | P0-01 boundary |
| :--- | :--- | :--- | :--- |
| Standalone | Operator application plus local backend/data adapter | Small or isolated installation | Current prototype direction; exact production persistence awaits P0-04 |
| Coordinated | Multiple clients plus an optional self-hosted coordination/API and shared metadata layer | Shared sites, control rooms, operators, alarms, jobs, and audit | Product contract only; service/auth/sync details are not yet approved |
| Scale-out | Coordinated profile plus optional workers, site agents, and distributed processing | Very large registered inventories and concurrent workloads | Architecture direction only; tested limits and partitioning remain later gates |

#### Data and Media Ownership

| Data/media | Authoritative owner or location | P0-01 rule |
| :--- | :--- | :--- |
| Continuous recordings | NVR/DVR/edge device or deployer-selected storage | Not copied into a mandatory VMS archive |
| Live media | Source device while an approved view/operation needs it | Transient and resource-budgeted |
| Playback media | Recorder/device/user-selected recording service | Requested on demand |
| Downloaded clips/evidence | Location explicitly selected or configured by the deployer | Separate evidence/retention gates apply |
| VMS inventory/configuration/health/alarm/job/audit metadata | Standalone local store or optional self-hosted shared store | Exact authority, synchronization, backup, and HA await later gates |
| Credentials/secrets | Deployer-controlled secure storage | Exact vaulting and access rules await later security gates |

#### Deliberately Deferred Decisions

| Deferred detail | Owning roadmap gate |
| :--- | :--- |
| Product/vendor integration boundary and open-source license selection | P0-02 / P0-17 |
| Outage behavior and high availability | P0-03 / P12-04 |
| Database choice, authority, synchronization, backup, and restore | P0-04 / P1-10 |
| Hardware/codec profiles, numeric capacity, and optimization thresholds | P0-05 through P0-13 / P12-03 |
| Security, privacy, authentication, enforced roles, and audit implementation | P0-16 / P1-02 / P1-03 / P1-06 |
| APIs, queues, background workers, load balancing, and site-agent protocols | P1-05 / P7-03 |
| High/medium/low/idle transitions, budgets, fairness, and latency | P2-13 / P3-03 / P6-08 / P12-03 |
| Recorder playback/download and evidence behavior | P4-03 / P5-01 / P5-02 / P5-08 / P5-09 |

#### Current-Project Consistency Evidence

- The current loopback backend and browser-local fallback are a temporary **Standalone** prototype, not the coordinated/scale-out implementation.
- Existing main/sub/thumb/paused media policy and per-recorder stream guards align with resource-conscious viewing, but do not implement the approved high/medium/low/idle device-activity model.
- Current JSON/localStorage full-snapshot persistence is not a scalable target and must not be presented as one.
- Real recorder live/playback/download bridges, centralized coordination, authentication/RBAC enforcement, capacity reporting, and scale-out workers are not implemented by P0-01 and remain gated work.
- No mandatory vendor cloud or telemetry dependency exists in the current runtime.

#### P0-01 Verification Checklist - 2026-07-15

- [x] The controlling roadmap records explicit owner approval and all P0-01A through P0-01L decisions as accepted.
- [x] Standalone, optional self-hosted coordinated, and scale-out profiles are defined without claiming that unbuilt services already exist.
- [x] Continuous recorder-owned footage, transient live media, on-demand playback/download, VMS metadata, evidence, and credentials have distinct ownership boundaries.
- [x] Administrator is the only prototype role; legacy prototype role records normalize to Administrator with the complete current capability registry.
- [x] Stable capability identifiers are retained so later RBAC can assign features without redesigning their boundaries.
- [x] Device activity priorities are documented separately from existing main/sub/thumb/paused per-view media tiers.
- [x] Project documentation consistently states that JSON/localStorage is a temporary standalone adapter and that database/coordinator choices remain later gates.
- [x] No mandatory vendor cloud, subscription, or telemetry dependency was introduced.
- [x] JavaScript syntax checks and the complete dependency-free VMS regression suite pass.
- [x] Product code for unapproved database, coordinator, HA, RBAC enforcement, device-priority scheduling, capacity optimization, or media bridges was not added.

- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **P0-02 [Architecture] Product boundary.** The project owner approved an independent, vendor-neutral core with one identity-only reference adapter. The versioned boundary, exact registry, identity-only shell, normalized outcome safeguards, neutral port handling, documentation, and tests are implemented and verified. Real vendor-specific, ONVIF, RTSP, SDK, and cloud operations remain unapproved until their individual evidence gates pass.

### P0-02 Accepted Vendor-Boundary Contract

- The product is an independent, vendor-neutral VMS. Its first registered integration is an identity-only reference adapter, not the product identity, an exclusive dependency, or a claim of current device compatibility.
- The core owns normalized sites, devices, cameras/channels, groups, users/capability identifiers, jobs, events, health records, playback requests, evidence records, errors, UI workflows, persistence contracts, media policy, and auditing concepts.
- Optional adapters own vendor/protocol identity, authentication/session details, device/model/firmware interpretation, discovery, channel/profile translation, status/events/errors, and approved device operations.
- Adapter selection must use an explicit adapter identifier. Free-text vendor labels and generic device types must not select an adapter. There is no implicit vendor-adapter, ONVIF, or other fallback.
- The first registered adapter is an **identity-only reference shell**. Until later evidence and implementation gates are approved, its discovery, health, firmware, events, PTZ, live, playback, download, and configuration operations remain **unknown/not verified**.
- Adapter outcomes must distinguish succeeded, failed, unsupported, unknown, and unavailable. Missing/unverified adapter support must not be recorded as a false offline device or failed physical command.
- Other vendors can later implement the same versioned adapter contract without edits to core dispatch logic. Each real integration still requires its own evidence, security, protocol, device/firmware, licensing, and feature approval.
- The project may use documented open standards and properly authorized vendor APIs/SDKs after verification. It must not call undocumented/private cloud endpoints, bypass activation/authentication/licensing/security controls, redistribute protected vendor code/assets without permission, or claim affiliation/certification/complete compatibility without evidence.

#### P0-02 Implementation Boundary

| Included now | Deliberately deferred |
| :--- | :--- |
| Versioned adapter manifest and operation contract | Production domain/schema migration and persisted adapter assignment (P0-04/P1-04) |
| Exact registry with explicit adapter IDs and collision checks | Dynamic per-device capability negotiation and feature flags (P1-07) |
| Async normalized operation outcomes | Worker/service/event topology (P1-05) |
| Identity-only reference adapter shell | ONVIF or vendor-specific discovery, authentication, and channel import (P0-14/P2-03/P2-05/P2-14) |
| Unknown/unsupported/unavailable safeguards | Live media, PTZ, diagnostics, playback and downloads (P3/P4/P5) |
| Arbitrary second-adapter and no-fallback tests | Model/firmware compatibility and vendor support claims (P0-14/P12-02) |
| Explicit device port validation in the common core | Adapter-specific port suggestions and onboarding UX (P2-01/P2-11) |

#### Third-Party Naming and Licensing Boundary

All third-party names and marks belong to their respective owners. This independent project makes no affiliation, endorsement, certification, or compatibility claim without documented evidence. Exact names are recorded only when required to identify an approved adapter, standard, source, entitlement, or compatibility result. The repository records an open-source intention but has no selected `LICENSE`; license selection and third-party/SDK redistribution review remain later owner decisions under P0-17.

#### P0-02 Verification Checklist - 2026-07-15

- [x] Adapter selection requires an explicit adapter ID; free-text vendor and device type are ignored by dispatch.
- [x] The identity-only reference shell is the first and only default registered adapter and declares no verified operations.
- [x] Missing or unknown adapter IDs never fall back to a vendor adapter, ONVIF, or another protocol.
- [x] Version/ID/alias/operation validation rejects malformed or colliding adapters.
- [x] Operation results distinguish succeeded, failed, unsupported, unknown, and unavailable.
- [x] Unregistered or unverified operations do not create false offline health records or failed physical-command records.
- [x] An arbitrary second test adapter executes asynchronously without changes to core dispatch logic.
- [x] The shared UI/API/service layer requires and preserves an explicit device port instead of injecting port 8000.
- [x] Common device-management wording is vendor-neutral and existing vendor values are treated as metadata/demo fixtures.
- [x] Documentation makes no real vendor compatibility, private API, cloud, certification, or endorsement claim.
- [x] JavaScript syntax checks and the complete dependency-free VMS regression suite pass.
- [x] No real vendor network call, SDK, credential flow, protocol bridge, discovery, live stream, playback, download, PTZ, or firmware implementation was added under P0-02.

- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **P0-03 [Architecture/NFR] Local, server, cloud, and hybrid failure behavior.** The project owner authorized adoption of the proposed decisions where standards-compatible and required replaceable modular implementation. The corrected contract, versioned policy layer, current-prototype honesty safeguards, focused tests, browser verification, and full regression suite are complete.

### P0-03 Accepted Failure-Behavior Contract

> The normative contract is maintained in [`docs/failure-behavior.md`](docs/failure-behavior.md). P0-03 defines behavior and replaceable policy boundaries. It does not claim that later-gate collectors, databases, queues, media bridges, authentication, access control, clusters, or VMS-owned recording already exist.

#### Owner Authorization and Standards Corrections

- The owner authorized the recommended P0-03 decision whenever it is compatible with standard implementation practice and asked that any conflict be reported.
- No proposed direction was prohibited by the reviewed standards. Six overly absolute formulations were corrected before implementation:
  - Continuous VMS monitoring requires a verified source-near continuity provider, not a hard-coded separate site-agent process.
  - The VMS must not synthesize or replay a late access grant; the controller follows its separately approved local/offline/life-safety policy after an authorization timeout.
  - A VMS ingestion ID prevents duplicate delivery after first acceptance but cannot prove that similar source events without a stable source ID are the same occurrence.
  - Blocking unauditable privileged/physical actions is the configurable secure default, not a universal standards mandate; autonomous life-safety and device operation remain non-blockable.
  - Health is multidimensional; stale last-known data must not be presented as current online evidence.
  - Retry rules are shared, but scheduling and budgets may be distributed per site/dependency instead of creating one global bottleneck.
- A locally calculated download hash proves later local integrity only. Equality with the source requires a trusted source digest or strong stable validator.

| Sub-item | Topic | Approved decision |
| :--- | :--- | :--- |
| P0-03A | Product and recording boundary | The VMS is an optional data-acquisition, normalization, manipulation, presentation, and interaction layer. Existing cameras, NVRs, DVRs, storage, and controllers remain autonomous. A VMS outage does not itself stop their native functions. Current continuous footage remains recorder/device owned. Optional local/remote/distributed VMS recording is deliberately deferred and must later plug in through a replaceable recording provider. |
| P0-03B | Failure isolation and continuity | Failure isolation applies inside the VMS: one failed site, device, adapter, malformed response, stream, import, or job must not unnecessarily block or falsify unrelated VMS work. Continuous VMS-side collection while the UI/WAN/coordinator is unavailable requires a declared continuity provider; its eventual process topology remains a later gate. |
| P0-03C | Authority, inventory, and configuration | A last-known-good inventory may remain visible with explicit freshness and read-only status. Writes requiring an unavailable authority must fail explicitly and must not be silently queued or reported saved. Offline conflict resolution and database technology remain P0-04 work. |
| P0-03D | Live, playback, search, and download | Media is opened only when required and when its real source/path/capability/authorization is available. Recorder-owned recording remains independent. Unreachable sources make recording state unknown unless direct fresh evidence proves otherwise. Transfer resume is capability- and validator-gated; incomplete or unverified files are never presented as complete evidence. |
| P0-03E | Commands and access | Read-only operations may use bounded safe retries. Physical, momentary, or destructive commands are live-only, expire, are never queued for reconnect replay, and report `outcome unknown` after ambiguous delivery. Controller-local/offline/life-safety behavior remains authoritative and separately approval-gated. |
| P0-03F | Alarms and audit | Future alarm/audit continuity uses durable acceptance, stable ingestion identity, source identity/sequence where supplied, at-least-once delivery, duplicate-safe central ingestion, original/receive/sync times, delayed labels, and explicit possible-duplicate/gap markers. Secure audit failure defaults may block privileged/physical VMS actions but remain configurable; viewing, autonomous recording, egress, and life-safety are not blocked by VMS audit failure. |
| P0-03G | Health and root cause | Reachability, freshness, functional health, adapter/capability state, recording, and storage remain separate dimensions. Parent unreachability makes dependent observations unknown/stale, not proven offline or not-recording. One root-cause incident represents a shared failure instead of generating a false alarm per child. |
| P0-03H | Resource pressure and recovery | Registered inventory does not imply active connections or streams. A protected system-critical lane precedes high/medium/low/idle work. Overload sheds idle/background/transfers before eligible interactive media and preserves alarms/audit capacity. Recovery uses bounded retry classification, injected timing/capacity limits, jitter, per-site/dependency budgets, and ordered restoration without delayed unsafe-command replay. |
| P0-03I | Optional services and licences | WAN, cloud, vendor-service, or optional licensed-component failure affects only functions declaring that dependency. Direct local/vendor-neutral paths continue when their actual dependencies remain available. The open-source core has no expiry or mandatory licence service, and optional-component failure must not lock inventory or evidence. |
| P0-03J | Modularity and later replacement | Availability, operation safety, health, recovery, and resource policies use stable versioned contracts with dependency injection and pure state calculation. Concrete storage, transport, workers, adapters, UI, and services can later be replaced without changing callers or core policy IDs. Unknown future functions default conservatively until explicitly classified. |

#### P0-03 Implementation Boundary

| Included now | Deliberately deferred |
| :--- | :--- |
| Versioned `shared/resilience/` contract and replaceable availability registry | Production database, authority, synchronization, snapshot, backup, or conflict implementation (P0-04/P1-10) |
| Explicit operation-safety, retry, queue, expiry, audit, and ambiguous-outcome policies | Authentication, offline-session, RBAC, credential, and final audit-storage implementation (P0-16/P1-02/P1-03/P1-06) |
| Multidimensional health derivation and root-cause helpers | Real device polling, alarm collection, device history, and final health persistence/UI domain (P1-08/P1-09/P2-13/P6) |
| Pure recovery ordering, distributed budget keys, injected full-jitter calculation, and transfer-resume decisions | Automatic retry workers, queues, coordinators, site-process topology, and numeric retry/capacity defaults (P1-05/P7-03/P12-03) |
| System-critical/high/medium/low/idle work ordering and staged degradation contract | Production auto-tuning, measured limits, decoder/network budgets, and full priority scheduler (P0-05 through P0-13/P12-03) |
| Injectable `backend/resilience/` facade | Real cloud/vendor services, access controllers, media bridges, playback/download engines, clusters, and failover (P2 through P5/P8/P12-04) |
| Honest prototype safeguards: unknown defaults, no inherited child-offline claim, no fake PTZ queue, visible inventory authority/save states, durable-save failure reporting | VMS-owned local/remote/distributed continuous recording and its storage/reconciliation/failover design (later recording/storage gates) |

#### P0-03 Verification Checklist - 2026-07-17

- [x] Native device operation and recorder-owned recording remain explicitly outside the VMS failure path.
- [x] A missing continuity provider disables continuous VMS monitoring without implying any physical-device failure.
- [x] Cached inventory becomes stale/read-only when its authority is unavailable; dependent writes are blocked explicitly.
- [x] WAN/coordinator/vendor-service/licence failure affects only functions that declare those dependencies.
- [x] Physical/destructive operations cannot be queued or replayed; expiry and ambiguous `outcome unknown` behavior are explicit.
- [x] The secure audit-failure default is injectable and replaceable rather than hard-coded as a universal standard.
- [x] Health tests keep reachability, freshness, function, recording, storage, and adapter state separate.
- [x] Parent failure makes child state unknown/stale, preserves unrelated trees, and produces one root-cause record.
- [x] Recovery and resource policies preserve system-critical work, use distributed budget keys, and accept injected numeric limits.
- [x] Download resume requires verified capability and stable source identity; local hashing does not overclaim source equality.
- [x] Adapter operations can distinguish ordinary failure from a post-send unknown outcome.
- [x] Frontend PTZ reports unavailable instead of queued until a verified adapter operation exists.
- [x] New/generated device and camera status defaults are unknown; recorder status is not copied as direct camera evidence.
- [x] Failed server-backed inventory persistence preserves an explicit unsaved draft, pauses further edits, and never claims a confirmed save.
- [x] Automatic backend persistence fallback cannot report a successful service mutation or durable inventory save.
- [x] README and backend documentation describe implemented policy boundaries without claiming deferred services.
- [x] JavaScript syntax checks, focused P0-03 policy/integration tests, the complete dependency-free VMS regression suite, and local browser verification pass.

- [x] Discussed | [x] Approved | [x] Built | [x] Verified *(approved standalone first pass)* - **P0-04 [Architecture] Authoritative data and persistence.** Select an embedded local relational store, central database, cloud service, or synchronized edge/server ownership; define migrations, conflict resolution, ordered duplicate-safe offline reconciliation, backup, restore, and export. **Scope approved 2026-07-26** ([`native/persistence-P0-04-proposal.md`](native/persistence-P0-04-proposal.md)): the owner selected the **embedded, standalone-first** direction — an embedded local relational store (**SQLite**, the selected embedded technology) as the standalone local authority behind the C0-03 adapter contract; versioned forward migrations; the entities the native client needs now (devices, cameras, `credential_ref`, camera groups, operator settings, stream profiles, workspace layout); backup/restore/export; honest failure per P0-03C. Central database, cloud, synchronization, and ordered duplicate-safe offline reconciliation are deferred to the coordinated gate (P1-10); credential secrets (DPAPI) are the next gate. Built as native increment 5. **The approved standalone first pass is built and verified on the dev box (2026-07-26):** `vms_persist` — an embedded SQLite store (via the Windows-bundled winsqlite3, no download/vendoring, Qt-free, swappable) behind the C0-03 adapter contract (declared tables, atomic writes with RAII rollback, versioned idempotent forward migrations in a single-source-of-truth `Schema.cpp`, typed persistence errors, online backup/export) with a `WorkspaceRepo` for layout state (5a/5b), and `vms_workspace` wired to persist and restore each stateful instance's layout (5c). `vms_dbtest`/CTest `persist_selfcheck` passes 29 checks covering acceptance §8 (canonical schema, durability across reopen, foreign-key constraint, atomic rollback, layout round-trip, export); 5c is verified end-to-end (close the app at a 25-tile layout, relaunch requesting `--count 4`, it restores 25). The deferred coordinated work — central DB/cloud, synchronization, ordered duplicate-safe offline reconciliation — remains **P1-10**, and credential **secrets** (DPAPI) are the next approved gate (connection broker + credential store).
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-05 [NFR] Supported workstation and codec profile.** Approve operating-system families, CPU/RAM/GPU baselines, hardware acceleration, H.264/H.265 behavior, any vendor-specific codec extensions, resolution/frame-rate assumptions, and graceful degradation as decode capacity is approached. Establish profiles through current measurements rather than copied legacy hardware tables.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-06 [NFR] Local device and media capacity profile.** Define and measure supported local encoder, storage service, media service, camera, group, per-group channel, decoder, and concurrent-media limits for named deployment profiles.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-07 [NFR] Local operator and display capacity profile.** Define and measure standard/wide-screen layouts, auxiliary displays, synchronized-playback channels, active users, and map capacity for each supported workstation profile.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-08 [NFR] Local access and security capacity profile.** Define and measure organization depth, person and credential counts, access groups, authorization templates, and security zones after the governing data and privacy rules are approved.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-09 [NFR] Enterprise video capacity profile.** Define independently tested limits for directly managed devices, cameras, remote-site cameras, specialized camera interfaces, concurrent operators, and active media work.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-10 [NFR] Enterprise endpoint and security capacity profile.** Define independently tested limits for intercom, signage, visitor, patrol, alarm I/O, radar, security-area/zone, face-reader, and vehicle-recognition endpoints without inheriting a vendor product ceiling.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-11 [NFR] Enterprise analytics and event capacity profile.** Define independently tested analytics-channel, active-rule, attachment, and alarm-event throughput profiles, including overload behavior and honest degradation.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-12 [NFR/Policy] Enterprise identity and biometric capacity profile.** Define independently tested portrait, match-throughput, access-profile, door, schedule, and card-layout limits only after biometric and access-data governance is approved.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-13 [Architecture/Commercial] Separate four kinds of limits.** Model technical maximums, purchased entitlements, configured tenant/site quotas, and independently validated deployment profiles as different values.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-14 [Integration] Vendor evidence gate.** For every device adapter, cloud-managed service, enterprise VMS, remote support portal, third-party map provider, ARC, CVR, SAN, or third-party database capability, record the supported API/SDK/protocol, version, rate limits, device firmware, license, and source verification date.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-15 [Policy/Commercial] Countries, service regions, and data residency.** Confirm target countries and service regions in writing; verify data residency, cross-border transfer, telecommunications, export, subscription, and feature-availability constraints for each approved dependency. Do not repeat source-guide regional claims without dated evidence.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-16 [Policy] Security, privacy, and safety baseline.** Approve handling for video, audio, credentials, faces/biometrics, GPS, ANPR, attendance, temperature/mask data, X-ray imagery, remote unlock/barrier actions, broadcasting, firmware, password reset, retention, deletion, legal hold, and cross-border transfer.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-17 [Commercial] Licensing and entitlement scope.** Decide whether the product needs license enforcement or only deployment documentation for video, access control, intercom, visitor, attendance, mustering, remote-site, mobile, signage, video-wall, vehicle-recognition, face/body, analytics/reporting, evidence, augmented-reality, temperature/mask, security-inspection, support, and bundle capabilities.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P0-18 [Benefit/NFR] Measurable product outcomes.** Convert claims such as one unified interface, less empty-footage review, low latency, data sovereignty, no customer-managed central server, no VPN/public IP, lower capital cost, predictable operating cost, proactive support, and no-recurring-fee local operation into measurable, topology-specific acceptance criteria.

## Phase 1 - Core Platform Foundation

- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P1-01 [Feature] Tenant, customer, site, area, organization, and group hierarchy.** Define ownership, nesting, movement, deletion, and cross-site visibility.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P1-02 [Feature/Policy] Users, super user, technician subaccounts, roles, and least privilege.** Include site-scoped permissions, privileged-action separation, activity tracking, revocation, and session expiry.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P1-03 [Feature/Policy] Authentication and privileged confirmation.** Define password policy, MFA or step-up authentication, recovery, lockout, service accounts, secrets storage, and approval for dangerous remote actions. *Note: only the **device secrets-storage** clause is being realized early, behind the N0 connection-broker decision, by the scope-approved **connection broker + credential store (DPAPI)** gate (see Approval Log, 2026-07-27; native increment 6, `native/credential-broker-P1-03-proposal.md`). Operator authentication, password policy, MFA/step-up, recovery, lockout, and service accounts remain unapproved under this P1-03 gate.*
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P1-04 [Architecture] Domain model and database migrations.** Define stable identifiers and lifecycle rules for devices, cameras, channels, groups, streams, sites, users, events, recordings, persons, credentials, doors, maps, licenses, and integrations.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P1-05 [Architecture] API, adapter, event-bus, and background-job boundaries.** Keep device protocols, UI, persistence, alarms, schedules, analytics, and vendor services independently testable.
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P1-06 [Feature/Policy] Immutable audit history.** Record logins, configuration changes, live/export access, credential and biometric use, remote commands, acknowledgements, vendor calls, entitlement changes, and before/after values with trusted timestamps. *(Native increment 28 built the durable append-only SHA-256 command chain and source-attributed read/verification surfaces. Native increment 44, 2026-08-09, adds schema v15 and a backward-compatible canonical v2 covering structured category/actor/subject/correlation plus redacted before/after evidence; it also adds an RFC 3161-ready SHA-256 chain-head request and admits a timestamp receipt only after injected cryptographic verification and unchanged-head confirmation; `--audit-selftest` 28 checks; scope `native/audit-structure-anchor-P1-06-proposal.md`. Remaining: wire producers for login/credential/biometric/live/export/vendor/entitlement events as those features exist; production TSA/trust-root/revocation policy; and retention that prunes only externally anchored prefixes while preserving a verifiable checkpoint.)*
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P1-07 [Architecture] Configuration, capability negotiation, and feature flags.** Unsupported features must be visibly unavailable; risky/vendor capabilities need scoped flags and rollback controls.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P1-08 [NFR] Time, time zone, clock drift, and localization.** Define machine instants, site-local wall time, operator input/display formats, daylight-saving behavior, device synchronization, ambiguous/nonexistent local times, and ordering of offline events. Existing `YYYY-MM-DD`, `HH:MM:SS`, and `YYYY-MM-DD HH:MM:SS` text are prototype proposals, not an approved universal storage format.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P1-09 [NFR] Observability and diagnostics.** Define logs, metrics, traces, health states, correlation IDs, operator-facing errors, maintenance suppression, and redaction of secrets/personal data. *(Native increment 29, 2026-07-31: the **secret-redaction** clause is realized early where it was actively being violated — a command parameter marked `secret` (plus a password/token/credential name backstop) is rendered as `***` in every audit row, console log, and catalog entry, while the handler still receives the real credential; verified by a raw byte-scan of the durable audit DB after an end-to-end onboard. The rest of P1-09 — metrics, traces, correlation IDs, operator-facing error taxonomy — remains unapproved.)*
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P1-10 [Feature] Safe configuration import, export, backup, and recovery.** Define validation, preview, atomic commit, version compatibility, partial failure, and rollback.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P1-11 [Feature/Architecture] Canonical entity, location, tag, and alias data.** Define stable entity/site and location/area identities, many-to-many camera coverage, searchable tags, typed aliases, merge/history behavior, snapshots, cross-site visibility, and API/storage mappings without requiring one physical central database.
- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **P1-12 [Architecture] Unified command envelope and executor.** Route UI clicks, gestures, keyboard actions, deterministic voice parsing, and any later assistant through one validated command contract with capability checks, target resolution, confirmation, result mapping, and audit boundaries. *(Native increment 25, 2026-07-31, under the A0 mandate + standing directive: the typed envelope, deterministic text leg, machine-readable catalog, and Command palette were built & verified. Native increment 29 routed the remaining shipped QML mutation sites through the structured envelope and added a failing coverage gate. Native increment 43, 2026-08-09, closes P1-12 with a recognizer-neutral deterministic voice mapper: registered BCP-47 grammar tags, confidence/ambiguity/no-match refusal, typed workspace/playback/alarm/device intents, separate destructive confirmation, and source-attributed audit; `command_selfcheck` 50 + `--command-selftest` 40 checks; scope `native/voice-command-P1-12-proposal.md`. The built-in command surface is now covered across UI, text, and recognized-speech invocation.)*
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P1-13 [Architecture/Feature] Complete command coverage and external control API (mandate A0).** *(Native increment 26, 2026-07-31: the **external control API's first slice** is built & verified — a loopback-only, opt-in, bearer-authenticated HTTP/JSON transport over the P1-12 envelope with catalog discovery (`GET /v1/commands`), validated+capability-scoped+confirm-gated+audited invocation (`POST /v1/invoke`), and deterministic status mapping; proven by an external client driving the running app; scope `native/command-api-P1-13-proposal.md`. Native increment 29, 2026-07-31: the **complete-coverage check** is built & verified — a `CoverageCheck` over the shipped QML that exits non-zero if any state-changing UI action bypasses the command layer, with all 27 former direct call sites rerouted through a new structured `invoke(id,args,confirm)` path and 12 new commands; declared exemptions are printed, never silent. Native increment 30, 2026-08-01: the explicitly deferred **Playback transport command slice** is built & verified — nine `playback.*` / `replay.*` verbs route both transport surfaces through the same capability/audit/API gate; committed seeks are audited and no `DeferredSlice` coverage exemption remains; scope `native/playback-command-P1-13-proposal.md`. Native increment 42, 2026-08-09: the **request-rate-limit hardening slice** is built & verified — every loopback HTTP route shares a default/configurable fixed 60-second budget, excess traffic receives structured 429 backpressure, and rejected invocations cannot mutate workspace state; scope `native/api-rate-limit-P1-13-proposal.md`. Remaining: scoped identities/tokens and TLS (coordinated with P1-02/P1-03), streaming, and the AI-agent connector.)* Every state-changing action in the product has a registered command with a stable identifier, human/voice/text invocation, and machine-readable descriptor (parameters, capability requirement, whether it is dangerous/physical/confirmable, and result shape). Expose these over a stable, authenticated, capability-scoped, audited external API (with discovery of the command catalog) so an external AI agent can drive the software with the same authority model and confirmation gates as a human operator. Define transport, versioning, auth, rate limits, dangerous-action step-up, streaming of results/events, and a coverage check that fails if any UI action bypasses the command layer. Deterministic (non-AI) voice/text parsing remains the built-in fallback; the AI agent is a client of this API, not a privileged path.

## Phase 2 - Device, Channel, and Site Onboarding

- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P2-01 [Feature] Recorder and direct-camera onboarding.** Approve workflows and required fields for NVR, DVR, hybrid DVR, and direct IP camera, including secure credentials and one-channel direct-camera behavior. *(Native increment 10, 2026-07-29: the standalone inventory-layer first pass — `DeviceRepo` atomic onboard with credential via the SecretStore/broker scheme — is built & verified. Native increment 14, 2026-07-29: the **Onboarding UI** — a `DeviceController` + Devices tab in `vms_workspace` for onboard / honest health / acknowledge / maintenance / remove — is built & verified (`--devices-selftest`, offscreen smoke). Native increment 15, 2026-07-29: **NVR/hybrid multi-channel onboarding** — schema v5 `devices.kind`, `onboardRecorder` (channel-count + URL-template expansion), form kind selector — is built & verified (24-check `--devices-selftest`). Native increment 16, 2026-07-30: **discovery as a data source** — an injectable `DiscoverySource` wires the ONVIF WS-Discovery + media SOAP cores into the Devices tab (scan → profile→Main/Sub mapping → one-click atomic onboard, with duplicate detection) — is built & verified offscreen (36-check `--devices-selftest`); the live LAN scan/fetch remains the hardware run.)*
- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **P2-02 [Feature] Automatic device group.** On every successful NVR, DVR, hybrid DVR, or direct IP-camera add, create or reconcile exactly one protected default group tied to the stable device ID and containing that device's current camera channels. *(Native increment 10, 2026-07-29: `DeviceRepo` creates/reconciles exactly one default group per device on onboard and on channel sync; verified in `device_selfcheck`.)*
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P2-03 [Feature/Integration] LAN discovery and activation.** Define autodetection, duplicate detection, activation state, supported subnets, authorization, safe bulk selection, and discovery timeout. *(Native increment 11, 2026-07-29: standards-exact ONVIF WS-Discovery — Probe build + prefix-agnostic ProbeMatch parse + duplicate detection + discovery timeout — is built & verified against real device fixtures (`vms_onviftest`); the live LAN multicast transport is build-verified and awaits a hardware run against the test camera. Native increment 16, 2026-07-30: the **discovery→onboard UI** surfaces discovered devices in the Devices tab with **duplicate detection** against the inventory and one-click atomic onboard (behind an injectable `DiscoverySource`, offscreen-verified, 36-check `--devices-selftest`). Activation state, subnet scoping, authorization, and safe bulk selection remain.)*
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P2-04 [Feature/Integration] Manual, profile-import, P2P, remote-site, and cloud onboarding.** Make connection method and telemetry limitations explicit; require entitlement and site authorization where applicable.
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P2-05 [Feature] Channel and stream-profile synchronization.** Add, update, reorder, rename, disable, and remove channels idempotently while preserving stable camera IDs and operator-customized metadata. *(Native increment 10, 2026-07-29: `DeviceRepo::syncChannels` — idempotent add/update/remove preserving stable ids and operator-customized names — is built & verified. Native increment 17, 2026-07-30: the **per-channel management UI** — schema v6 `cameras.disabled`, `DeviceRepo` listChannels/renameChannel/setChannelDisabled/removeChannel with group reconciliation from the enabled set, and an expandable Channels panel in the Devices tab (rename / enable-disable / remove) — is built & verified (46-check `--devices-selftest`, offscreen smoke migrated v5→v6 clean). Native increment 18, 2026-07-30: **channel reorder** (schema v7 `cameras.sort_order`, `reorderChannels`, ▲/▼ UI) + **stream-profile sync from a discovery source** (`onboardDiscovered` stores the chosen ONVIF profiles' resolution/codec into `stream_profiles`, shown per channel) — is built & verified (51-check `--devices-selftest`, offscreen smoke migrated v6→v7 clean). The full P2-05 verb set is now built & offscreen-verified; live ONVIF confirmation shares the hardware block.)*
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P2-06 [Feature] Rename, rescan, detach, and delete behavior.** Preserve associations on rename, detach configured cameras safely, clean stale group/routes, handle reduced channel counts, and require confirmation for destructive effects. *(Native increment 20, 2026-07-30, approved by owner directive: **device rename preserving associations** (`DeviceRepo::renameDevice` — name + default-group name refreshed, cameras/ids/operator-names/group-membership/credential untouched) and a **confirmation gate on the destructive Remove** are built & verified (62-check `--devices-selftest`, offscreen smoke). Native increment 21, 2026-07-30: **device attach/detach** (schema v8 `devices.disabled`; `setDeviceDisabled` empties the default group while preserving all config; `pollHealth` skips detached devices; Attach/Detach UI + dimmed card + "detached" badge) is built & verified (68-check `--devices-selftest`). Native increment 22, 2026-07-30: **rescan a camera from discovery** (`rescanDevice` re-fetches a direct camera's ONVIF profiles from the matched discovered device and reconciles its channel, preserving the operator name + stable id; refuses recorders) is built & verified (73-check `--devices-selftest`). P2-06's buildable scope is complete (rename · remove-confirmation · attach/detach · rescan); per-channel detach/rename remains.)*
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P2-07 [Integration] Camera capability families.** Standard, fisheye, panoramic, PTZ, thermal, ANPR/LPR, face/body, people-counting, queue, heat-map, passenger-counting, solar, and third-party cameras must advertise only proven capabilities.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P2-08 [Integration] Infrastructure devices.** Support the approved subset of storage servers, stream-media servers, CVR/SAN endpoints, decoders, video-wall outputs, auxiliary displays, and recording servers.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P2-09 [Integration] Physical-security endpoints.** Support the approved subset of doors, controllers, readers, turnstiles, face readers, alarm panels, inputs/outputs, radars, intercom door/indoor stations, speakers, signage, visitor kiosks, patrol docks, X-ray scanners, and metal detectors.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P2-10 [Integration] Mobile and field devices.** Support the approved subset of body-worn cameras, mobile/vehicular DVRs, GPS telemetry, solar cameras, and cellular data units.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P2-11 [Feature/Integration] Network configuration.** Define IP, ports, DNS, DDNS, proxy, NAT/P2P, certificates, connectivity tests, configuration validation, and authorization boundaries.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P2-12 [Integration/Policy] Remote support actions.** Separately approve log retrieval, password reset, configuration change, reboot, and batch firmware update with site authorization, preflight checks, step-up authentication, progress, failure recovery, and rollback.
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P2-13 [Feature] Device health and lifecycle.** Define online/offline/degraded/unsupported states, storage and stream health, exceptions, firmware inventory, maintenance windows, notification, acknowledgement, and clear/recovery behavior. *(Native increment 13, 2026-07-29: the `HealthMonitor` decision core — honest online/offline/degraded/unsupported states from separate reachability/stream/storage dimensions, exceptions with acknowledge + auto-clear/recovery, maintenance-window suppression, firmware inventory, notification work-list — is built & verified (`vms_healthtest`). Native increment 14, 2026-07-29: the health UI (Devices tab). Native increment 19, 2026-07-30: the **live polling loop** — an injectable `DeviceHealthProbe` (live `QTcpSocket` reachability probe + fixture fake) drives `HealthMonitor` automatically via `DeviceController::pollHealth()` on a timer, so a device goes Unknown→Online/Offline and recovers on its own (56-check `--devices-selftest`; live TCP confirmation on the LAN is the hardware tail). The notification transport (P6-08) and a media-open stream-health probe remain.)*
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P2-14 [Integration] Interoperability.** Decide ONVIF profiles and any other approved standards plus explicit vendor adapters; document supported profiles, models, firmware, authentication, conformance evidence, and behavior when proprietary features are unavailable. *(Native increments 11–12, 2026-07-29: standards-exact ONVIF WS-Discovery + the device/media SOAP client — WS-Security UsernameToken PasswordDigest, GetDeviceInformation/GetProfiles/GetStreamUri — are built & verified against real device fixtures (`vms_onviftest`); live device fetch + `DeviceRepo` onboarding awaits a hardware run. Native increment 16, 2026-07-30: the discover→onboard **orchestration** — an injectable `DiscoverySource` + a pure `GetProfiles`→Main/Sub channel mapping wired into the onboarding UI — is built & verified offscreen; the live `FetchDevice()`→`DeviceRepo.onboard` remains the hardware run. Vendor-specific adapters, the supported-profile/firmware conformance matrix, and proprietary-feature fallbacks remain.)*

## Phase 3 - Live Monitoring, Display, and Operator Response

- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-01 [Feature] Live-view workspace and custom layouts.** Define drag/drop, saved/personal/shared layouts, camera/group/site navigation, empty/error states, and standard/wide-screen window divisions. *(Native increment 24, 2026-07-31: the **drag verb** landed on the spatial canvas — a camera tile is dragged to a new position and the position persists across restarts (schema v9). Saved/personal/shared named layouts, grid drag/drop, and the rest of this gate remain undiscussed.)*
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-02 [Feature/NFR] Multi-display operation.** Define the supported local and browser auxiliary-display profile, focus, pop-out recovery, bandwidth/decode budgets, and persisted placement through measured workstation testing.
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P3-03 [Feature/NFR] Stream selection and decode governor.** Choose main/sub/adaptive streams, latency targets, reconnect policy, hardware acceleration, resource limits, and operator warnings before overload. **Scope approved 2026-07-24** ([`native/governor-P3-03-proposal.md`](native/governor-P3-03-proposal.md)): first pass is a two-budget capacity model (decode-throughput + memory) seeded from `vms_hwprobe`, per-tier cost (main/sub/thumb/paused), admission control, and degrade/recover with hysteresis; full P0-01L priority transitions, bandwidth, multi-monitor, and persistence deferred. Building as native increment 4b.
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P3-04 [Feature] Live diagnostics.** Show bitrate, frame rate, resolution, codec, latency, packet loss, and stream state where supported; display an explicit unavailable state for P2P/cloud paths that do not provide telemetry. *(Native increment 34, 2026-08-03: the standalone diagnostics contract and current governed-media observation path are built & verified — per-branch playing/connecting/stalled/paused state, compositor→Qt observed FPS, active codec/resolution, a dedicated tile-id-indexed read model, and a selected-camera diagnostics overlay. Unsupported bitrate/latency/packet-loss fields are typed unavailable with source-specific reasons, never fake zeroes. Release 64-tile smoke observed 16/16 active branches at 29.7 FPS and 48/48 paused tiles without stale data; `--diagnostics-selftest` + performance/regression gates green. Remaining for the full gate: populate transport fields from a real source exposing RTSP/RTP/RTCP or equivalent telemetry.)*
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P3-05 [Feature] Instant playback from live view.** Define look-back duration, storage source, permissions, transition back to live, and missing-footage behavior. *(Native increment 23, 2026-07-31, owner-selected lane + standing directive: the standalone first pass is built & verified — `InstantReplayController` reuses the recording `PlaybackController`/`PlaybackPipeline` to jump back 10/30/60 s on the recording-backed camera from the Live info panel into a layered replay overlay over the still-running wall, with a transport + return-to-live; honest per P0-03 — clamps a too-long look-back to the earliest recorded moment and reports the actual duration, and shows an unrecorded camera as unavailable without ever presenting requested time as recorded; `--instant-selftest` 22 checks + offscreen smoke pulled 89 real decoded frames; scope `native/instant-playback-P3-05-proposal.md`. **Look-back duration** = fixed 10/30/60 s presets; **storage source** = the local recording index only (P0-01E). Deferred: arbitrary live-tile→`camera_id` mapping (a later media gate), recorder/NVR/cloud instant retrieval, permissions/RBAC, and evidence capture.)*
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-06 [Feature/Integration] Fisheye display and dewarping.** Approve Panorama, PTZ, Half Sphere, AR Half Sphere, and Cylinder modes, client/device-side processing, controls, and performance limits.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-07 [Feature/Integration] PTZ and panoramic master-slave control.** Define manual PTZ, presets, tours, permissions, ownership conflicts, panoramic-to-PTZ coordination, and audit.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-08 [Feature/Integration] Target tracking.** Define supported devices/analytics, start/stop/manual override, lost-target behavior, false-positive handling, and visible tracking state.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-09 [Feature/Integration/Policy] Auxiliary device controls.** Separately approve strobe, hazardous-gas routine, and de-icing heater actions, including capability checks, interlocks, confirmation, timeout, feedback, and audit.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-10 [Feature/Integration/Policy] Live audio, intercom talk, and operator announcements.** Define half/full duplex, device selection, echo/feedback behavior, authorization, recording/consent, emergency priority, and audit.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-11 [Feature/Integration/Policy] Scheduled and event-driven mass broadcast.** Define speaker groups, content approval, schedules, priority/preemption, failed endpoints, cancellation, and safety controls.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-12 [Feature/Integration] Video walls and physical decoder outputs.** Define video-wall layouts, decoder/output routing, auxiliary GPU rendering, alarm-feed takeover, restoration, and hardware resource validation.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-13 [Feature/Integration] Digital signage.** Define display/touchscreen inventory, playlists, schedules, publishing, preview, offline cache, emergency override, and endpoint limits.
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P3-14 [Feature/Architecture] Shared layered Live and Playback workspace UI.** Use two independently stateful instances of one responsive component family. The camera grid fills the workspace background; resource browser, camera/group search, selected-camera information/control, tabs, and toolbars layer over it; the application rail remains outside. Playback adds its range, timeline, transport, availability, and evidence controls without forking shared components. **Slice 1 built & verified on the dev box (2026-07-26, native `vms_workspace`):** a Qt Quick window binds the real `GovernorSession` to a grid whose per-tile border + status strip render the honest P3-03 per-tile state (green live / amber degraded / red capacity-paused / slate off-screen), with an aggregate capacity meter, an over-capacity warning, and a live focus-sweep re-plan. This is the first layered-grid element and it realizes the P3-03 acceptance #5 "honest state, on screen" requirement; it depends only on Qt Quick + the pure `vms_governor` library (no GStreamer/D3D) and verifies headlessly (`--selftest`) and windowed. **Slice 2a (2026-07-26) adds the gst→Qt Quick video bridge** (`--video`): a `VideoItem` (`QQuickItem`) fed by a GStreamer `appsink` uploads each RGBA frame to a scene-graph texture so the state chrome draws over live video (`qml6glsink` is absent from the MSVC binaries, so this `appsink`→`QSGTexture` route is the deliberate interop-free bridge). **Slice 2b-1 (2026-07-26) makes the governed d3d11-composited grid the real video source** (`GridPipeline`: per-tile hardware decode → `d3d11compositor` → `d3d11download` → `appsink` → `VideoItem`), built from the governor's initial plan, chrome aligned cell-for-cell, paused tiles composited black — verified on the dev box with 16 tiles decoding real H.265 (`d3d12h265dec`, hardware) under the honest chrome (lowend: 1 main/live + 1 sub/degraded + 14 thumb/degraded). **Slice 2b-2 (2026-07-26) re-plans the live video on a focus sweep in lock-step with the chrome:** one timer drives both — the controller re-plans the chrome and `GridPipeline::applyPlan` reconfigures the live video to the same tiers (whole-graph NULL→rebuild-fronts→PLAYING, the reliable path vms_grid uses; sweepable grids pre-encode every tier; a no-op when nothing changed) — verified with `--video --sweep-interval 3` (the MAIN/focused tile moves across the grid in step, `d3d12h265dec`, no re-plan errors). **Slice 2b-3 (2026-07-26) makes `--profile auto` the default and seeds the governor from the live hardware probe** (D3D per-codec capability + a registered GStreamer hardware decoder, exactly as `vms_grid`): on the dev box it reports `auto h265 (High, hardware decode, NVIDIA GeForce RTX 3050)` and plans 25 main + 39 sub of 64 (matching `vms_grid`) — completing the P3-14 slice-2 group (live governed video under the honest chrome). **Slice 3 (2026-07-26) adds the first workspace interaction and the first layered panel:** clicking any tile focuses it (`WorkspaceController::focusTile`) — the governor protects it at Main and degrades the rest around it — a `planChanged` signal makes both the chrome and (under `--video`) the live picture follow, a "selected-camera information" panel layers over the grid (id · tier · state · priority), and a click stops the auto-sweep so the operator holds the working set (verified: `--selftest` promotes the picked tile to Main; a simulated click moved focus+MAIN+panel+video together). This is the first operator→governor input the whole design exists to serve. **Slice 4 (2026-07-26) adds the layered resource browser and a toolbar:** a camera-list panel with a live search filter layers over the grid (each row: state dot · `Cam N` · tier·state, click-focuses that camera, focused one highlighted) and a toolbar toggles the browser and the auto sweep (`WorkspaceController::setAutoSweep`/`autoSweeping`) — pure Qt Quick primitives, no new Qt module, verified over the live 16-tile governed video with the browser list state matching the tiles exactly. **Slice 5 (2026-07-26) adds the second orthogonal control axis (ARCHITECTURE.md):** operator-settable device priority, decoupled from focus so an operator's mark persists across sweeps (`WorkspaceController::setPriority`, `focused` and `priority` now independent), set via High/Med/Low buttons in the info panel with a ★ badge on High cameras — under capacity pressure the governor degrades lower-priority cameras first (verified: a non-focused High camera holds Main while a Low one is paused, tile 1 High=main / tile 15 Low=paused on lowend-16). **Slice 6 (2026-07-26) adds runtime layout control:** toolbar presets (4/9/16/25/64) resize the wall live (`WorkspaceController::setTileCount`) — the working set is rebuilt and re-planned, and under `--video` the whole grid pipeline is rebuilt for the new geometry via a `layoutChanged` signal (encoded clips are cached in `GridPipeline` so the rebuild is instant); fewer tiles let the governor upgrade more to Main (verified: 2 main at 4 tiles vs 1 main at 16, real decoded video throughout). **Slice 7 (2026-07-26) makes the first orthogonal axis interactive:** operator-set desired media tier (`WorkspaceController::setDesiredTier`, Main/Sub/Thumb/Off buttons in the info panel) — the governor seeds each tile at `desired` and never exceeds it, so capping a camera or turning it Off frees decode+memory budget for the others (verified: a Thumb cap holds at thumb and Off pauses even with budget to spare). Both orthogonal control axes from ARCHITECTURE.md (per-view media tier + device priority) are now operator-controllable. **Slice 8 (2026-07-26) realizes the P3-14 headline — two independently stateful instances of one workspace:** a Live/Playback tab bar backed by two `WorkspaceController` instances, with the QML `governor` bound to the active tab so the entire view re-binds to that instance's state with no duplication; the video pipeline follows the Live instance, and the Playback tab is chrome-only with a range/timeline/transport scaffold (timeline track + transport controls) honestly marked "awaiting recorded footage (Phase 4)". Verified by capturing both tabs (Live: video-backed governed grid; Playback: independent state + transport bar, video hidden). The Phase-4 recording/index/decode path subsequently wired this range/timeline/transport to actual recorded footage, and increment 30 routes its discrete controls through the command envelope/API with audited committed seeks. The remaining gate work is the seamless (non-reloading) live apply and later synchronized/evidence playback slices.
- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **P3-15 [Feature] Spatial site navigation and viewport media policy.** Define premises/floor canvases, camera position/facing/field of view, pan/zoom, visible/focus/peripheral/offscreen behavior, pre-warm limits, map pins versus video tiles, persistence, and performance acceptance. *(Native increment 24, 2026-07-31, owner directive "same or better than the prototype": the **spatial canvas + viewport media policy** first pass is built & verified — a Grid \| Spatial toggle on the Live workspace, prototype-exact pan/zoom/zones/zoom-levels (`SpatialPolicy.h`), pre-warm margin, map-pin dots at site zoom, per-tile drag with persisted positions (schema v9), and the policy driving the real governor so offscreen/culled tiles genuinely stop decoding; `--spatial-selftest` 33 checks + offscreen smoke + v8→v9 migration. The later increment updates below supersede this first-pass remaining list and close the approved standalone scope.)*
  **Increment 31 update (2026-08-01):** the canonical active site/floor model, optional floor-plan underlay, timezone read-out, persisted camera facing/FOV, audited configuration commands, and coverage wedges are now built and verified under `native/premises-spatial-P3-15-proposal.md` (schema v11). This supersedes those items in the older “remaining” sentence above. The epic stays unchecked until measured performance acceptance and the map-pin-vs-live-video-tile decision/media implementation are complete; multi-site administration remains separately scoped.
  **Increment 32 update (2026-08-01):** controller-side measured performance acceptance is now built and verified: deterministic 64-tile idle and forced-replan gates pass in Debug and Release, with a clean 64-tile offscreen QML load. The only remaining P3-15 product slice is the separately scoped map-pin-versus-live-video spatial media decision/implementation; physical-display/GPU and low-end measurements remain hardware evidence rather than inferred claims.
  **Increment 33 update (2026-08-03):** the progressive spatial media decision is implemented and verified: site pins, wing state cards, and room imagery only for governor-admitted live/degraded tiles, sourced as cell crops from the single governed composite with no duplicate sessions or decodes. This completes P3-15's approved standalone product scope. Real-camera-source confirmation and physical low-end/display timing remain explicit hardware evidence gates, not unverified product claims.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-16 [Feature] Operator-led tracking sessions and breadcrumbs.** Define case type, subject descriptor, priority, active/paused/resolved state, concurrent cases, camera/time breadcrumbs, notes, resume, escalation, incident linkage, permissions, retention, and audit.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P3-17 [Feature/Integration/Policy] Manual route graph and camera-candidate hints.** Let operators record directional transitions and show a small ranked candidate set using site geometry and approved history. Any re-identification or predictive hint remains optional, confidence-labeled, human-reviewed, privacy-gated, and never an automatic source of truth.
- [x] Discussed | [x] Approved | [x] Built | [x] Verified - **P3-18 [Feature] Front-layer per-entity operational, analysis, and device panel.** *(Owner-directed requirement, 2026-07-29.)* On the workspace's **front/overlay layer** (the P3-14 layered model — over the camera stage, alongside the selected-camera info panel), show, per **entity/premises**: its **operations time**, its **analysis data**, and its **device information**. This builds on the P3-14 selected-camera info panel and pulls in P3-04 live diagnostics (bitrate/fps/resolution/codec/latency/stream state), P2-13 device health/lifecycle (online/offline/degraded, firmware), P3-15 premises/site model, and P5/P6 analytics/event data. **Owner clarifications (2026-07-29):** the **entity** the panel keys on is the **premises/site**; **"operations time"** means **all four** of: (a) site-local time + time zone (P0-01I), (b) operating hours / open-closed status, (c) online uptime + last-seen, and (d) cumulative operating (recording/streaming) duration. *(Native increment 35, 2026-08-03: the first standalone panel foundation is built & verified — active site/floor/timezone and valid local clock, honest device-health aggregates, governed Live media counts/FPS, and explicit reasoned unavailable states for operating hours/open status, uptime/last-seen, cumulative duration, and analytics. The panel is read-only and front-layered; `--site-operations-selftest`, persistent video smoke, performance, and regressions pass. Native increment 36, 2026-08-03: schema v12 weekly/special-date hours, audited configuration commands, explicit UTC→IANA DST semantics, and the panel's real Open/Closed state are built & verified; Release smoke restored `hours=Open` and all schedule/DST/regression gates pass. Native increment 37, 2026-08-03: schema v13 device→site ownership, monotonic reachability observations, audited assignment, restart-honest uptime/last-seen, and truly site-scoped device aggregates are built & verified; Release smoke restored six assigned devices and four current reachability runs. Native increment 38, 2026-08-03: canonical completed-segment time is unioned per current active-site camera and exposed with overlap/evidence counts; Release record→index→panel smoke observed 5 seconds across two segments. Remaining for the full gate: persistent streaming-duration accounting and specific approved analytics sources/fields.)*

  **Increment 39 update (2026-08-05):** the final sentence above is superseded for streaming duration. Schema v14 persistent decoded branch-time, monotonic `playing`-only accounting, site-switch/restart isolation, and separate/combined panel evidence are built & verified; two Release runs advanced the same premises total across reopen without filling downtime. The only remaining P3-18 source decision is now the specific approved analytics fields and their authority.

  **Increment 40 update (2026-08-09):** the standalone analysis authority is the native alarm engine joined to current device→site ownership. The panel now reports site-scoped active device-health alarm attention, severity, lifecycle, occurrence, and affected-device counts, visibly labeled current-session only. It excludes VCA and retained trend claims. This completes P3-18's approved standalone scope; future VCA, historical analytics, and cross-site reporting require their own P3/P5/P6 decisions.

## Phase 4 - Recording, Storage, Retention, and Recovery

- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P4-01 [Feature] Main-stream and sub-stream recording profiles.** Define codec, resolution, frame rate, bitrate, audio, source capability, synchronized dual-stream behavior, and storage estimates.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P4-02 [Feature] Continuous, event-driven, and command-triggered schedules.** Define calendar precedence, holidays, pre/post event buffers, manual override, conflicts, and audit.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P4-03 [Feature/Architecture] Local recording and recording-server ownership.** Define edge/NVR/server/workstation destinations, gaps, index authority, failover, and retrieval priority.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P4-04 [Integration] CVR and SAN allocation.** Define provisioning, quotas, disk pools, compatibility, capacity forecasts, failure states, and safe reallocation.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P4-05 [Feature] Main and auxiliary storage.** Define primary/secondary routing, replication or independent roles, health, failover, restore, and reconciliation.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P4-06 [Feature/Policy] Retention loops and deletion.** Define per-camera/event retention, protected evidence, overwrite order, legal hold, low-space behavior, deletion proof, and time-based cleanup.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P4-07 [Integration/Policy] Encrypted cloud recording.** Separately approve event-only and continuous recording, 7/30-day or custom loops, transport/at-rest encryption, key ownership, region, export, restore, deletion, and outage behavior.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P4-08 [Integration] Cloud backup for direct IP, solar, and NVR sources.** Define supported upload paths, bandwidth control, retries, duplicate prevention, missing segments, and entitlement behavior.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P4-09 [NFR] Storage health and resilience.** Detect disk/server/cloud failures, forecast exhaustion, suppress maintenance noise, alert within the agreed SLO, recover without silent loss, and expose recording gaps.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P4-10 [NFR] Backup, restore, migration, and vendor exit.** Prove metadata/configuration backup, recording/evidence export, integrity checks, migration across versions/topologies, and recovery objectives.

## Phase 5 - Playback, Search, and Digital Evidence

- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P5-01 [Feature/NFR] Synchronized remote playback.** Define the validated concurrent-channel profile, audio sync, scrubbing, speed, frame-step, buffering, drift tolerance, and partial-camera failure.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P5-02 [Feature] Playback range, footage timeline, and recording-source selection.** Keep start/end date-time selection and draggable navigation; merge continuous/event segments and local/recorder/server/cloud sources while distinguishing requested, available, missing, unknown, overlapping, and duplicate footage instead of hiding gaps or presenting requested time as recorded.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P5-03 [Feature] Standard, alarm-input, and event search.** Define filters, site/camera/time scoping, pagination, saved searches, result previews, and permissions.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P5-04 [Feature/Integration] ATM, POS, and VCA retrieval.** Define source adapters, transaction/rule indexing, time correlation, data redaction, retention, and failure states.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P5-05 [Feature/Integration] Human and vehicle event filtering.** Define metadata source, confidence threshold, false-positive behavior, unsupported-camera state, and measurable time saved versus manual review.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P5-06 [Feature/Integration/Policy] Face, body, and ANPR search.** Define authorized purpose, indexes, query fields, confidence, list matching, audit, retention, and manual fallback.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P5-07 [Feature/Integration/Policy] Natural-language and metadata search.** Define voice, text, and target-class search requirements; confirm actual API/service availability, supported languages, measurable accuracy/latency, explainability, privacy, entitlement, and deterministic non-AI fallback.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P5-08 [Feature/Policy] Incident and evidence management.** Define secure archive, case association, tag generation, bookmarks, notes, file locking, legal hold, access review, and retention.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P5-09 [Feature/Policy] Evidence export and chain of custody.** Define clip/snapshot formats, watermark/hash/signature, transcoding, redaction, player packaging, authorization, export audit, and integrity verification.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P5-10 [Feature] Reverse time/location search and playback staging.** Search a site, entity, location, area, tag, and time range; resolve all covering cameras; open them in synchronized Playback; promote a found moment into an operator tracking session; and preserve query-to-playback evidence without assuming footage exists.

## Phase 6 - Events, Alarms, Verification, and Automation

- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P6-01 [Feature] Security areas, partitions, and zones.** Define hierarchy, arming states, bypass, ownership, schedules, permissions, and device synchronization.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P6-02 [Integration] Alarm inputs and outputs.** Define normalization, debounce, restore/tamper states, output duration, feedback, device loss, and safe manual control.
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P6-03 [Feature/NFR] Event and alarm rule engine.** Define triggers, conditions, schedules, suppression, correlation, priorities, dependencies, simulation, versioning, and an independently measured active-rule profile. *(Native increment 27, 2026-07-31, standing directive: the **first rule-engine pass** is built & verified — pure `AlarmEngine` with event-type triggers + device filters, enable/disable, priority mapping, auto-clear events, and maintenance suppression, fed by live device-health transitions; `event_selfcheck` 30 checks; scope `native/events-alarms-P6-proposal.md`. Remaining: schedules, correlation, dependencies, simulation, versioning, and the measured active-rule profile.)*
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P6-04 [NFR] Alarm ingestion and storage pipeline.** Define attachment behavior, backpressure, ordering, deduplication, persistence, recovery, and an independently measured event-throughput profile.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P6-05 [Feature/Policy] Automated linkage.** Separately approve camera pop-up/recording, PTZ, door, barrier, alarm output, strobe, speaker, signage, notification, and external dispatch actions with loop prevention and fail-safe defaults.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P6-06 [Feature] Alarm verification workspace.** Define related live/recorded video, audio, map, access/ANPR context, operator checklist, acknowledgement, escalation, resolution, and evidence capture.
- [x] Discussed | [x] Approved | [ ] Built | [ ] Verified - **P6-07 [Feature/NFR] Alarm lifecycle and notifications.** Define new/acknowledged/escalated/cleared states, assignment, deduplication, maintenance suppression, delivery channels, retry, SLA/SLO, and audit. *(Native increment 27, 2026-07-31: the **lifecycle core** is built & verified — new→acknowledged (persists across re-ingest)→escalated→cleared with deduplication, auto-clear on recovery, fresh-alarm-after-clear, maintenance suppression (notification, never state), and the lifecycle verbs routed through the audited A0 command envelope (`alarm.ack/escalate/clear` — panel, palette, and external API are one gate); `--alarms-selftest` 17 checks end-to-end. Remaining: assignment, delivery channels, retry, SLA/SLO, durable audit tie-in (P1-06).)*
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P6-08 [Integration] Health events and linkage.** Normalize device/storage/stream/firmware exceptions, notify and clear reliably, and enforce country/entitlement restrictions on automated actions.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P6-09 [Integration/Policy] Standard ARC receiver.** Confirm protocol/API, panel mapping, heartbeat, acknowledgement, duplicate/event ordering, authorization, tenancy, and audit.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P6-10 [Integration/Policy] Premium ARC, video verification, and dispatch.** Define evidence packet, operator confirmation, dispatch contract, failure/escalation, jurisdiction, and strict fail-closed behavior.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P6-11 [Feature/Policy] Compliance types, events, review logs, and overlays.** Define preset/freeform type governance, entity/location association, extracted versus operator-confirmed records, historical camera/location snapshots, manager responses, top-occurrence overlays, reporting windows, permissions, privacy, retention, and audit.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P6-12 [Feature/Integration/Policy] External-message ingestion and query tickets.** Through an approved connector, convert designated messages into traceable tickets with raw-source reference, entity/location, requested range, subject, confidence, status, authorization, correction, deduplication, retention, and outage behavior. No private endpoint or direct multi-process database writer is permitted.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P6-13 [Feature] Ticket-driven playback, tracking, and resolution.** Resolve mapped cameras, stage the requested range in Playback, link an operator tracking session and breadcrumbs, record findings and manager response, then resolve or escalate the ticket without losing provenance.

## Phase 7 - Maps, Remote Sites, AR, and Mobile Operations

- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P7-01 [Feature] E-Maps.** Define image/floor-plan upload, camera/door/alarm placement, layers, status, navigation, permissions, versioning, and an independently measured map-capacity profile.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P7-02 [Feature/Integration] GIS site and camera mapping.** Define map provider, exact/approximate GPS, clustering, live status, privacy, offline behavior, rate limits, and map-license terms.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P7-03 [Feature/Integration/NFR] Remote Site Management.** Define site federation, delegated administration, event/video routing, health, time zones, entitlement, isolation, partitioning, and independently measured site/camera scale profiles.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P7-04 [Integration] P2P/cloud-connected cameras.** Define authorization, connection state, privacy, entitlement, reconnect, limited telemetry, bandwidth, and local fallback without assuming a public API.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P7-05 [Feature/Integration] AR high-point scenes.** Define scene calibration, adjacent camera/gate tags, visual overlays, interactions, permissions, per-scene entitlement, and fallback when calibration is invalid.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P7-06 [Feature/Integration/Policy] Mobile recorder and vehicle mapping.** Define GPS update rate, history, privacy, device status, playback correlation, geofences, and loss-of-signal behavior.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P7-07 [Feature/Integration/Policy] Route, speed, and driver analytics.** Define route planning, deviation, speed thresholds, driver identity/behavior, alerts, retention, labor/privacy rules, and dispute workflow.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P7-08 [Integration] Patrol docking stations.** Confirm actual workflow/API, evidence ingest, officer/device assignment, charging/upload status, exceptions, retention, and the approved endpoint count.

## Phase 8 - Access Control, Intercom, Visitors, Attendance, and Vehicles

- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-01 [Feature/Policy] Person and credential lifecycle.** Define enrollment, approval, activation, expiry, suspension, deletion, deduplication, consent, import/sync, and audit.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-02 [Feature/Integration/Policy] Credential types.** Separately approve cards, reader PINs, fingerprints, face, iris, Bluetooth, dynamic QR, NFC, and mobile credentials with fallback and anti-sharing controls.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-03 [Feature] Doors, readers, controllers, access groups, schedules, and card templates.** Define topology, permissions, holidays, conflicts, preview, deployment, versioning, and edge synchronization.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-04 [Feature/Integration/Policy] Remote and mobile unlock.** Define authorized roles, step-up confirmation, reason capture, door state/feedback, timeout, retries, emergency behavior, notification, and audit.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-05 [NFR/Policy] Offline edge access.** Define approved outage duration, cached credential security, clock behavior, revocation limits, local audit capacity, complete ordered reconciliation, and conflict handling.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-06 [Feature/Policy] Advanced door logic.** Separately approve multi-door interlocking, anti-passback, first-person-in, and custom Wiegand mapping with simulations, override, fail-safe/fail-secure choices, and safety review.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-07 [Feature/Integration] Video intercom.** Define door/indoor stations, direct call routing, door-to-app two-way calls, missed calls, privacy, video/audio handling, remote release, and event linkage.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-08 [Feature/Policy] Visitor management.** Define preregistration, identity checks, host approval, self-service kiosk, badge printing, temporary credentials, escort/watchlist policy, checkout, retention, and reporting.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-09 [Feature/Policy] Attendance.** Define shifts, clock events, compliance, vacation approval, reports, corrections, third-party HR/database sync, privacy, and separation from access authorization.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-10 [Feature/Policy] GPS/mobile attendance verification.** Define geofences, spoofing controls, user consent, video/access correlation, offline handling, disputes, retention, and jurisdiction.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-11 [Feature/Policy] Emergency mustering.** Define fire-release routing, muster lists/checklists, authoritative occupancy, offline operation, manual override, safety certification, drills, and post-event evidence.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P8-12 [Feature/Integration/Policy] ANPR vehicle access and parking.** Define reads/confidence, allow/block lists, temporary passes, remote barrier action, search, parking/payment calculations, anti-tailgate, manual override, audit, and retention.

## Phase 9 - Analytics, Reporting, and Specialized Sensors

- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P9-01 [Feature/Integration/Policy] Face recognition.** Define enrollment/comparison purpose, groups, thresholds, watchlists, false-match handling, human confirmation, demographic fields, audit, retention, and legal approval.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P9-02 [NFR/Policy] Face database and match pipeline.** Define storage/partitioning, encryption, deletion, quality controls, and independently measured portrait-capacity and match-throughput profiles.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P9-03 [Feature/Integration/Policy] Body-attribute matching.** Define supported attributes, prohibited inference, confidence, human review, retention, audit, and measurable bias/accuracy criteria.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P9-04 [Feature/Integration] People counting.** Define zones/lines, occupancy, reset/reconciliation, accuracy, privacy, reporting, and device/server analytics ownership.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P9-05 [Feature/Integration] Queue analytics.** Define queue zones, waiting-time thresholds, alerts, aggregation, accuracy, privacy, and operational reporting.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P9-06 [Feature/Integration] Heat maps, crowd flow, and BI reports.** Define inputs, aggregation, dashboards, comparisons, export, accuracy, retention, and store/site privacy.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P9-07 [Feature/Integration] Passenger counting.** Define stereo-camera support, entry/exit reconciliation, route/time aggregation, accuracy, missing-data handling, and reports.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P9-08 [Feature/Integration/Policy] Thermal, temperature, and mask analytics.** Define calibration, anomaly thresholds, trend reports, violations, access linkage, false readings, health-data privacy, and regional legality.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P9-09 [Feature/Integration] Radar and PTZ-radar tracking.** Define target/event model, camera handoff, zones, calibration, false alarms, degraded state, and supported device count.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P9-10 [Feature/Integration/Policy] Security inspection.** Define X-ray imagery, item counts, metal-detector events, operator decisions, evidence, privacy, retention, hardware/API support, and safety boundaries.

## Phase 10 - Cloud and Integrator-Service Capabilities

- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P10-01 [Integration] Optional cloud-managed multi-site and multi-user tenancy.** Compare self-hosted coordination with any approved cloud-managed service; define provider responsibilities, tenant type, device import, roles, site isolation, quotas, audit, API availability, and exit behavior.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P10-02 [Benefit/NFR] No-VPN/no-public-IP connectivity.** Prove the approved path behind real NAT, DNS, proxy, firewall, bandwidth-loss, reconnect, and throttling conditions; document all edge, WAN, and vendor-cloud dependencies.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P10-03 [Integration] Proactive technician health monitoring.** Define authorization, polling/events, SLOs, deduplication, maintenance suppression, notification, remote diagnostics, recovery clearing, and tenant isolation.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P10-04 [Integration/Policy] Remote-support site authorization.** Treat active, scoped customer authorization as a hard prerequisite for remote support; define grant, expiry, revocation, sharing, audit, and fail-closed behavior.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P10-05 [Integration] Cloud attendance service.** Confirm whether this is an external integration or an in-product module, including identity sync, shifts/reports, tenancy, API, entitlement, privacy, and outage behavior.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P10-06 [Integration/Commercial] Cellular IoT/4G management.** Confirm provider/API, SIM activation, plans/usage, alerts, suspend/resume, device binding, roaming, regional support, billing, and security.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P10-07 [NFR] Cloud and license outage continuity.** Define grace periods and prove which local inventory, live view, recording, access, and alarms continue when the vendor service or subscription is unavailable.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P10-08 [Policy/NFR] Cloud security and vendor exit.** Verify encryption in transit/at rest, key ownership, hosting/data region, backups, restore, deletion, audit access, sub-processors, breach process, export formats, and termination behavior.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P10-09 [Policy] Periodic vendor/region revalidation.** Recheck licenses, free tiers, prices, regional availability, APIs, rate limits, device firmware, terms, and legal claims on an agreed cadence and before major releases.

## Phase 11 - Licensing, Service Operations, and Commercial Tools

- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P11-01 [Commercial/Feature] Entitlement engine.** If in scope, model activation, base/add-on prerequisites, quantities, tenant/site assignment, grace, expiry, renewal, downgrade, overage, audit, and offline behavior.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P11-02 [Commercial] Metering units.** Decide how camera, P2P camera, body-camera, thermal channel, speaker, door, indoor station, remote site, mobile unit, decoder output, ANPR channel, face/body channel, face channel, and AR scene entitlements are represented.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P11-03 [Commercial] Free tier and proof of concept.** If a third-party offering is in scope, verify its current proof-of-concept entitlement; define activation, limits, expiry/change behavior, data access, and migration to/from paid service without making the tier an architecture dependency.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P11-04 [Commercial] Monthly/yearly subscriptions and renewal.** Define billing responsibility, activation delay, renewal, tax/currency, grace, failed payment, downgrade, cancellation, refunds, support, and data access after expiry.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P11-05 [Commercial] Support and maintenance terms.** Decide whether support and feature-update periods are tracked in-product or only documented; verify included periods and renewal rules for each selected supplier.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P11-06 [Commercial] Bundles and procurement catalog.** Treat video/access-control bundles and part numbers as versioned procurement data, not product functionality; define source, owner, refresh, region, and effective dates if retained.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P11-07 [Feature/Integration] Product specification selector.** Decide whether commercial product selection belongs in this VMS; if approved, define catalog source, compatibility rules, versioning, and vendor ownership.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P11-08 [Feature/Integration] Project registration, quotations, and pricing.** Decide whether these integrator-business workflows belong in scope; if approved, define customer/project records, approvals, currencies/tax, revisions, export, API, and audit.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P11-09 [Feature/Integration] Warranty and RMA tracking.** Decide whether it belongs in scope; if approved, define serial/device linkage, entitlement lookup, case status, logistics, attachments, notifications, API, and data ownership.

## Phase 12 - Verification, Pilot, Release, and Revalidation

- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P12-01 [NFR] Per-item acceptance specification.** Every approved feature must have functional behavior, supported/unsupported states, permissions, error/failure behavior, measurable performance, audit evidence, and explicit non-goals before coding.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P12-02 [NFR] Device and firmware compatibility test matrix.** Cover approved NVRs, DVRs, hybrid DVRs, direct IP cameras, analytics cameras, storage/media/decoder devices, access/intercom/alarm endpoints, browsers, operating systems, and connection methods.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P12-03 [NFR] Capacity and performance certification.** Test the selected device/channel/user/layout/map/site/door/rule/analytics ceilings, decode profiles, storage load, search latency, and event throughput rather than copying vendor limits.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P12-04 [NFR] Network and resilience testing.** Cover packet loss, latency, low bandwidth, DNS/proxy/firewall/NAT, LAN/WAN/cloud outage, reconnect storms, server/device restart, database/storage failure, and clock drift.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P12-05 [Policy/NFR] Security testing.** Cover least privilege, MFA/step-up, site authorization, revocation, secrets, injection, request validation, tenancy isolation, audit integrity, encryption, and fail-closed privileged actions.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P12-06 [Policy/NFR] Privacy and safety validation.** Cover video/audio/biometric/GPS/ANPR/attendance/temperature retention, consent/notice, minimization, export/deletion, access review, remote physical actions, emergency workflows, and jurisdictional approval.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P12-07 [NFR] Evidence and recovery validation.** Prove recording gaps are visible, backups restore, exports verify, legal holds survive retention, audit trails are complete, migrations preserve IDs, and rollback does not corrupt state.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P12-08 [NFR] Accessibility, localization, and operator usability.** Validate keyboard/screen-reader support, color/status clarity, language, date/time/number formats, time zones, alarm overload, dangerous-action confirmation, and representative operator workflows.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P12-09 [NFR] Documentation and operations readiness.** Complete installation, configuration, security, privacy, backup/restore, upgrade/rollback, device compatibility, operator/admin training, monitoring, incident, vendor escalation, and support runbooks.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P12-10 [NFR] Lab, pilot, UAT, and release gates.** Require representative hardware/firmware/tenant/region lab evidence, limited-site pilot, user acceptance, monitored rollout, feature flags, migration, rollback, ownership, and final sign-off.
- [ ] Discussed | [ ] Approved | [ ] Built | [ ] Verified - **P12-11 [NFR/Commercial/Policy] Scheduled revalidation.** Record source versions and expiry dates, then revalidate vendor capability, API, firmware, license, region, security, privacy, and legal assumptions before major upgrades.

---

## Source Coverage and Evidence Record

The 2026-07-19 comparison with the supplied original 623-line research guide found:

- all 143 original P0-P12 feature and gate IDs present, with zero missing IDs and zero status differences;
- seven deliberately revised titles—P3-12, P5-02, P5-07, P10-01, P10-04, P11-05, and P11-06—using vendor-neutral or clearer capability wording without removing their scope;
- ten project-specific additions—P1-11, P1-12, P3-14 through P3-17, P5-10, and P6-11 through P6-13—plus the eight-item C0 consolidation track;
- source capability coverage across platform foundation and limits (P0-P2/P12), live operation (P3), recording/storage (P4), playback/evidence (P5), alarms (P6), maps/mobile (P7), access/intercom (P8), analytics (P9), optional cloud/integrator services (P10), and commercial tooling (P11);
- no vendor name retained in the controlling roadmap; exact names remain outside the repository unless a later evidence record or adapter specification requires identification.

The external guide remains outside the repository as discovery material. Its copied prose, product names, hardware tables, capacity figures, SKUs, prices/entitlements, regional claims, and bibliography are deliberately not reproduced here. Before a third-party or standards-based integration is approved, P0-14 requires a claim-level evidence record containing:

- evidence ID and capability;
- exact product/service/standard and public-safe label;
- version, mode, API/SDK/protocol/profile, device model, and firmware;
- license/entitlement, tenant, country/region, and support terms;
- primary source, source version/publication date, review date, reviewer, and confidence;
- expiry/revalidation date and the roadmap item that depends on the claim.

Feature ideas and factual systems are restated independently in this roadmap; source wording is not a product requirement. This editorial cleanup reduces avoidable third-party naming and copied-expression risk, but it is not legal clearance for a product name, integration claim, license, or distribution package.
